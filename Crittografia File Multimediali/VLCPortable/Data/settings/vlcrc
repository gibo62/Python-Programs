###
###  vlc 3.0.23
###

###
### lines beginning with a '#' character are comments
###

[visual] # Visualizer filter

# Effects list (stringa)
#effect-list=spectrum

# Video width (intero)
#effect-width=800

# Video height (intero)
#effect-height=500

# FFT window (stringa)
#effect-fft-window=flat

# Kaiser window parameter (virgola mobile)
#effect-kaiser-param=3.000000

# Show 80 bands instead of 20 (booleano)
#visual-80-bands=1

# Draw peaks in the analyzer (booleano)
#visual-peaks=1

# Enable original graphic spectrum (booleano)
#spect-show-original=0

# Draw the base of the bands (booleano)
#spect-show-base=1

# Base pixel radius (intero)
#spect-radius=42

# Spectral sections (intero)
#spect-sections=3

# V-plane color (intero)
#spect-color=80

# Draw bands in the spectrometer (booleano)
#spect-show-bands=1

# Show 80 bands instead of 20 (booleano)
#spect-80-bands=1

# Number of blank pixels between bands. (intero)
#spect-separ=1

# Amplification (intero)
#spect-amp=8

# Draw peaks in the analyzer (booleano)
#spect-show-peaks=1

# Peak extra width (intero)
#spect-peak-width=61

# Peak height (intero)
#spect-peak-height=1

[projectm] # libprojectM effect

# projectM preset path (stringa)
#projectm-preset-path=

# Title font (stringa)
#projectm-title-font=C:\WINDOWS\Fonts\arial.ttf

# Font menu (stringa)
#projectm-menu-font=C:\WINDOWS\Fonts\arial.ttf

# Video width (intero)
#projectm-width=800

# Video height (intero)
#projectm-height=500

# Mesh width (intero)
#projectm-meshx=32

# Mesh height (intero)
#projectm-meshy=24

# Texture size (intero)
#projectm-texture-size=1024

[goom] # Goom effect

# Goom display width (intero)
#goom-width=800

# Goom display height (intero)
#goom-height=500

# Goom animation speed (intero)
#goom-speed=6

[glspectrum] # 3D OpenGL spectrum visualization

# Video width (intero)
#glspectrum-width=400

# Video height (intero)
#glspectrum-height=300

[wall] # Wall video filter

# Number of columns (intero)
#wall-cols=3

# Number of rows (intero)
#wall-rows=3

# Active windows (stringa)
#wall-active=

# Element aspect ratio (stringa)
#wall-element-aspect=16:9

[panoramix] # Panoramix: wall with overlap video filter

# Number of columns (intero)
#panoramix-cols=-1

# Number of rows (intero)
#panoramix-rows=-1

# length of the overlapping area (in %) (intero)
#panoramix-bz-length=100

# height of the overlapping area (in %) (intero)
#panoramix-bz-height=100

# Attenuation (booleano)
#panoramix-attenuate=1

# Attenuation, begin (in %) (intero)
#panoramix-bz-begin=0

# Attenuation, middle (in %) (intero)
#panoramix-bz-middle=50

# Attenuation, end (in %) (intero)
#panoramix-bz-end=100

# middle position (in %) (intero)
#panoramix-bz-middle-pos=50

# Gamma (Red) correction (virgola mobile)
#panoramix-bz-gamma-red=1.000000

# Gamma (Green) correction (virgola mobile)
#panoramix-bz-gamma-green=1.000000

# Gamma (Blue) correction (virgola mobile)
#panoramix-bz-gamma-blue=1.000000

# Black Crush for Red (intero)
#panoramix-bz-blackcrush-red=140

# Black Crush for Green (intero)
#panoramix-bz-blackcrush-green=140

# Black Crush for Blue (intero)
#panoramix-bz-blackcrush-blue=140

# White Crush for Red (intero)
#panoramix-bz-whitecrush-red=200

# White Crush for Green (intero)
#panoramix-bz-whitecrush-green=200

# White Crush for Blue (intero)
#panoramix-bz-whitecrush-blue=200

# Black Level for Red (intero)
#panoramix-bz-blacklevel-red=150

# Black Level for Green (intero)
#panoramix-bz-blacklevel-green=150

# Black Level for Blue (intero)
#panoramix-bz-blacklevel-blue=150

# White Level for Red (intero)
#panoramix-bz-whitelevel-red=0

# White Level for Green (intero)
#panoramix-bz-whitelevel-green=0

# White Level for Blue (intero)
#panoramix-bz-whitelevel-blue=0

# Active windows (stringa)
#panoramix-active=

[clone] # Clone video filter

# Number of clones (intero)
#clone-count=2

# Video output modules (stringa)
#clone-vout-list=

[yuv] # YUV video output

# device, fifo or filename (stringa)
#yuv-file=stream.yuv

# Chroma used (stringa)
#yuv-chroma=

# Add a YUV4MPEG2 header (booleano)
#yuv-yuv4mpeg2=0

[wgl] # WGL extension for OpenGL

# GPU affinity (intero)
#gpu-affinity=-1

[vmem] # Video memory output

# Width (intero)
#vmem-width=320

# Height (intero)
#vmem-height=200

# Pitch (intero)
#vmem-pitch=640

# Chroma (stringa)
#vmem-chroma=RV16

[vdummy] # Dummy video output

# Dummy image chroma format (stringa)
#dummy-chroma=

[glwin32] # OpenGL video output for Windows

# Open GL/GLES hardware converter (stringa)
#glconv=

# Rendering intent for color conversion (intero)
#rendering-intent=1

# Display primaries (intero)
#target-prim=0

# Display gamma / transfer function (intero)
#target-trc=0

# Tone-mapping algorithm (intero)
#tone-mapping=3

# Tone-mapping parameter (virgola mobile)
#tone-mapping-param=0.000000

# Highlight clipped pixels (booleano)
#tone-mapping-warn=0

# Dithering algorithm (intero)
#dither-algo=-1

# Dither depth override (0 = framebuffer depth) (intero)
#dither-depth=0

# Tone-mapping desaturation coefficient (virgola mobile)
#tone-mapping-desat=0.500000

[gl] # OpenGL video output

# OpenGL extension (stringa)
#gl=

# Open GL/GLES hardware converter (stringa)
#glconv=

# Rendering intent for color conversion (intero)
#rendering-intent=1

# Display primaries (intero)
#target-prim=0

# Display gamma / transfer function (intero)
#target-trc=0

# Tone-mapping algorithm (intero)
#tone-mapping=3

# Tone-mapping parameter (virgola mobile)
#tone-mapping-param=0.000000

# Highlight clipped pixels (booleano)
#tone-mapping-warn=0

# Dithering algorithm (intero)
#dither-algo=-1

# Dither depth override (0 = framebuffer depth) (intero)
#dither-depth=0

# Tone-mapping desaturation coefficient (virgola mobile)
#tone-mapping-desat=0.500000

[flaschen] # Flaschen-Taschen video output

# Flaschen-Taschen display address (stringa)
#flaschen-display=

# Width (intero)
#flaschen-width=25

# Height (intero)
#flaschen-height=20

[drawable] # Embedded window video

[directdraw] # DirectX (DirectDraw) video output

# Use hardware YUV->RGB conversions (booleano)
#directx-hw-yuv=1

# Overlay video output (booleano)
#directx-overlay=1

# Use video buffers in system memory (booleano)
#directx-use-sysmem=0

# Use triple buffering for overlays (booleano)
#directx-3buffering=1

# Name of desired display device (stringa)
#directx-device=

[direct3d9] # Direct3D9 video output

# Use hardware blending support (booleano)
#direct3d9-hw-blending=1

# Pixel Shader (stringa)
#direct3d9-shader=

# Path to HLSL file (stringa)
#direct3d9-shader-file=

[direct3d11] # Direct3D11 video output

# Use hardware blending support (booleano)
#direct3d11-hw-blending=1

# Video Upscaling Mode (stringa)
#d3d11-upscale-mode=linear

# HDR Output Mode (stringa)
#d3d11-hdr-mode=auto

[transform] # Video transformation filter

# Transform type (stringa)
#transform-type=90

[sharpen] # Sharpen video filter

# Sharpen strength (0-2) (virgola mobile)
#sharpen-sigma=0.050000

[sepia] # Sepia video filter

# Sepia intensity (intero)
#sepia-intensity=120

[scene] # Scene video filter

# Image format (stringa)
#scene-format=png

# Image width (intero)
#scene-width=-1

# Image height (intero)
#scene-height=-1

# Filename prefix (stringa)
#scene-prefix=scene

# Directory path prefix (stringa)
#scene-path=

# Always write to the same file (booleano)
#scene-replace=0

# Recording ratio (intero)
#scene-ratio=50

[rotate] # Rotate video filter

# Angle in degrees (virgola mobile)
#rotate-angle=30.000000

# Use motion sensors (booleano)
#rotate-use-motion=0

[puzzle] # Puzzle interactive game video filter

# Number of puzzle rows (intero)
#puzzle-rows=4

# Number of puzzle columns (intero)
#puzzle-cols=4

# Border (intero)
#puzzle-border=3

# Small preview (booleano)
#puzzle-preview=0

# Small preview size (intero)
#puzzle-preview-size=15

# Piece edge shape size (intero)
#puzzle-shape-size=90

# Auto shuffle (intero)
#puzzle-auto-shuffle=0

# Auto solve (intero)
#puzzle-auto-solve=0

# Rotation (intero)
#puzzle-rotation=0

# Game mode (intero)
#puzzle-mode=0

[postproc] # Video post processing filter

# Post processing quality (intero)
#postproc-q=6

# FFmpeg post processing filter chains (stringa)
#postproc-name=default

[posterize] # Posterize video filter

# Posterize level (intero)
#posterize-level=6

[motionblur] # Motion blur filter

# Blur factor (1-127) (intero)
#blur-factor=80

[mirror] # Mirror video filter

# Mirror orientation (intero)
#mirror-split=0

# Direction (intero)
#mirror-direction=0

[hqdn3d] # High Quality 3D Denoiser filter

# Spatial luma strength (0-254) (virgola mobile)
#hqdn3d-luma-spat=4.000000

# Spatial chroma strength (0-254) (virgola mobile)
#hqdn3d-chroma-spat=3.000000

# Temporal luma strength (0-254) (virgola mobile)
#hqdn3d-luma-temp=6.000000

# Temporal chroma strength (0-254) (virgola mobile)
#hqdn3d-chroma-temp=4.500000

[grain] # Grain video filter

# Variance (virgola mobile)
#grain-variance=2.000000

# Minimal period (intero)
#grain-period-min=1

# Maximal period (intero)
#grain-period-max=48

[gradient] # Gradient video filter

# Distort mode (stringa)
#gradient-mode=gradient

# Gradient image type (intero)
#gradient-type=0

# Apply cartoon effect (booleano)
#gradient-cartoon=1

[gradfun] # Gradfun video filter

# Radius (intero)
#gradfun-radius=16

# Strength (virgola mobile)
#gradfun-strength=1.200000

[gaussianblur] # Gaussian blur video filter

# Gaussian's std deviation (virgola mobile)
#gaussianblur-sigma=2.000000

[fps] # FPS conversion video filter

# Frame rate (stringa)
#fps-fps=

[extract] # Extract RGB component video filter

# RGB component to extract (intero)
#extract-component=16711680

[erase] # Erase video filter

# Image mask (stringa)
#erase-mask=

# X coordinate (intero)
#erase-x=0

# Y coordinate (intero)
#erase-y=0

[deinterlace] # Deinterlacing video filter

# Streaming deinterlace mode (stringa)
#sout-deinterlace-mode=blend

# Phosphor chroma mode for 4:2:0 input (intero)
#sout-deinterlace-phosphor-chroma=2

# Phosphor old field dimmer strength (intero)
#sout-deinterlace-phosphor-dimmer=2

[croppadd] # Video cropping filter

# Pixels to crop from top (intero)
#croppadd-croptop=0

# Pixels to crop from bottom (intero)
#croppadd-cropbottom=0

# Pixels to crop from left (intero)
#croppadd-cropleft=0

# Pixels to crop from right (intero)
#croppadd-cropright=0

# Pixels to padd to top (intero)
#croppadd-paddtop=0

# Pixels to padd to bottom (intero)
#croppadd-paddbottom=0

# Pixels to padd to left (intero)
#croppadd-paddleft=0

# Pixels to padd to right (intero)
#croppadd-paddright=0

[colorthres] # Color threshold filter

# Color (intero)
#colorthres-color=16711680

# Saturation threshold (intero)
#colorthres-saturationthres=20

# Similarity threshold (intero)
#colorthres-similaritythres=15

[canvas] # Canvas video filter

# Output width (intero)
#canvas-width=0

# Output height (intero)
#canvas-height=0

# Output picture aspect ratio (stringa)
#canvas-aspect=

# Pad video (booleano)
#canvas-padd=1

[bluescreen] # Bluescreen video filter

# Bluescreen U value (intero)
#bluescreen-u=120

# Bluescreen V value (intero)
#bluescreen-v=90

# Bluescreen U tolerance (intero)
#bluescreen-ut=17

# Bluescreen V tolerance (intero)
#bluescreen-vt=17

[blendbench] # Blending benchmark filter

# Number of time to blend (intero)
#blendbench-loops=1000

# Alpha of the blended image (intero)
#blendbench-alpha=128

# Image to be blended onto (stringa)
#blendbench-base-image=

# Chroma for the base image (stringa)
#blendbench-base-chroma=I420

# Image which will be blended (stringa)
#blendbench-blend-image=

# Chroma for the blend image (stringa)
#blendbench-blend-chroma=YUVA

[ball] # Ball video filter

# Ball color (stringa)
#ball-color=red

# Ball speed (intero)
#ball-speed=4

# Ball size (intero)
#ball-size=10

# Gradient threshold (intero)
#ball-gradient-threshold=40

# Edge visible (booleano)
#ball-edge-visible=1

[antiflicker] # antiflicker video filter

# Window size (intero)
#antiflicker-window-size=10

# Softening value (intero)
#antiflicker-softening-size=10

[anaglyph] # Convert 3D picture to anaglyph image video filter

# Color scheme (stringa)
#anaglyph-scheme=red-cyan

[alphamask] # Alpha mask video filter

# Transparency mask (stringa)
#alphamask-mask=

[adjust] # Image properties filter

# Image contrast (0-2) (virgola mobile)
#contrast=1.000000

# Image brightness (0-2) (virgola mobile)
#brightness=1.000000

# Image hue (-180..180) (virgola mobile)
#hue=0.000000

# Image saturation (0-3) (virgola mobile)
#saturation=1.000000

# Image gamma (0-10) (virgola mobile)
#gamma=1.000000

# Brightness threshold (booleano)
#brightness-threshold=0

[swscale] # Video scaling filter

# Scaling mode (intero)
#swscale-mode=2

[sapi] # Speech synthesis for Windows

# Voice Index (intero)
#sapi-voice=-1

[freetype] # Freetype2 font renderer

# Font (stringa)
#freetype-font=Arial

# Monospace Font (stringa)
#freetype-monofont=Courier New

# Font size in pixels (intero)
#freetype-fontsize=0

# Relative font size (intero)
#freetype-rel-fontsize=0

# Text opacity (intero)
#freetype-opacity=255

# Text default color (intero)
#freetype-color=16777215

# Force bold (booleano)
#freetype-bold=0

# Background opacity (intero)
#freetype-background-opacity=0

# Background color (intero)
#freetype-background-color=0

# Outline opacity (intero)
#freetype-outline-opacity=255

# Outline color (intero)
#freetype-outline-color=0

# Outline thickness (intero)
#freetype-outline-thickness=4

# Shadow opacity (intero)
#freetype-shadow-opacity=128

# Shadow color (intero)
#freetype-shadow-color=0

# Shadow angle (virgola mobile)
#freetype-shadow-angle=-45.000000

# Shadow distance (virgola mobile)
#freetype-shadow-distance=0.060000

# Use YUVP renderer (booleano)
#freetype-yuvp=0

# Text direction (intero)
#freetype-text-direction=0

[stream_out_transcode] # Transcode stream output

# Video encoder (stringa)
#sout-transcode-venc=

# Destination video codec (stringa)
#sout-transcode-vcodec=

# Video bitrate (intero)
#sout-transcode-vb=0

# Video scaling (virgola mobile)
#sout-transcode-scale=0.000000

# Video frame-rate (stringa)
#sout-transcode-fps=

# Deinterlace video (booleano)
#sout-transcode-deinterlace=0

# Deinterlace module (stringa)
#sout-transcode-deinterlace-module=deinterlace

# Video width (intero)
#sout-transcode-width=0

# Video height (intero)
#sout-transcode-height=0

# Maximum video width (intero)
#sout-transcode-maxwidth=0

# Maximum video height (intero)
#sout-transcode-maxheight=0

# Video filter (stringa)
#sout-transcode-vfilter=

# Audio encoder (stringa)
#sout-transcode-aenc=

# Destination audio codec (stringa)
#sout-transcode-acodec=

# Audio bitrate (intero)
#sout-transcode-ab=96

# Audio language (stringa)
#sout-transcode-alang=

# Audio channels (intero)
#sout-transcode-channels=0

# Audio sample rate (intero)
#sout-transcode-samplerate=0

# Audio filter (stringa)
#sout-transcode-afilter=

# Subtitle encoder (stringa)
#sout-transcode-senc=

# Destination subtitle codec (stringa)
#sout-transcode-scodec=

# Destination subtitle codec (booleano)
#sout-transcode-soverlay=0

# Overlays (stringa)
#sout-transcode-sfilter=

# Number of threads (intero)
#sout-transcode-threads=0

# Picture pool size (intero)
#sout-transcode-pool-size=10

# High priority (booleano)
#sout-transcode-high-priority=0

[stats] # Writes statistic info about stream

# Output file (stringa)
#sout-stats-output=

# Prefix to show on output line (stringa)
#sout-stats-prefix=stats

[stream_out_standard] # Standard stream output

# Output access method (stringa)
#sout-standard-access=

# Output muxer (stringa)
#sout-standard-mux=

# Output destination (stringa)
#sout-standard-dst=

# Address to bind to (helper setting for dst) (stringa)
#sout-standard-bind=

# Filename for stream (helper setting for dst) (stringa)
#sout-standard-path=

# SAP announcing (booleano)
#sout-standard-sap=0

# Session name (stringa)
#sout-standard-name=

# Session description (stringa)
#sout-standard-description=

# Session URL (stringa)
#sout-standard-url=

# Session email (stringa)
#sout-standard-email=

[smem] # Stream output to memory buffer

# Time Synchronized output (booleano)
#sout-smem-time-sync=1

[setid] # Change the id of an elementary stream

# Elementary Stream ID (intero)
#sout-setid-id=0

# New ES ID (intero)
#sout-setid-new-id=0

# Elementary Stream ID (intero)
#sout-setlang-id=0

# Language (stringa)
#sout-setlang-lang=eng

[stream_out_rtp] # RTP stream output

# Destination (stringa)
#sout-rtp-dst=

# SDP (stringa)
#sout-rtp-sdp=

# Muxer (stringa)
#sout-rtp-mux=

# SAP announcing (booleano)
#sout-rtp-sap=0

# Session name (stringa)
#sout-rtp-name=

# Session category (stringa)
#sout-rtp-cat=

# Session description (stringa)
#sout-rtp-description=

# Session URL (stringa)
#sout-rtp-url=

# Session email (stringa)
#sout-rtp-email=

# Transport protocol (stringa)
#sout-rtp-proto=udp

# Port (intero)
#sout-rtp-port=5004

# Audio port (intero)
#sout-rtp-port-audio=0

# Video port (intero)
#sout-rtp-port-video=0

# Hop limit (TTL) (intero)
#sout-rtp-ttl=-1

# RTP/RTCP multiplexing (booleano)
#sout-rtp-rtcp-mux=0

# Caching value (ms) (intero)
#sout-rtp-caching=300

# SRTP key (hexadecimal) (stringa)
#sout-rtp-key=

# SRTP salt (hexadecimal) (stringa)
#sout-rtp-salt=

# MP4A LATM (booleano)
#sout-rtp-mp4a-latm=0

# RTSP session timeout (s) (intero)
#rtsp-timeout=60

# Username (stringa)
#sout-rtsp-user=

# Password (stringa)
#sout-rtsp-pwd=

[record] # Record stream output

# Destination prefix (stringa)
#sout-record-dst-prefix=

[mosaic_bridge] # Mosaic bridge stream output

# ID (stringa)
#sout-mosaic-bridge-id=Id

# Video width (intero)
#sout-mosaic-bridge-width=0

# Video height (intero)
#sout-mosaic-bridge-height=0

# Sample aspect ratio (stringa)
#sout-mosaic-bridge-sar=1:1

# Image chroma (stringa)
#sout-mosaic-bridge-chroma=

# Video filter (stringa)
#sout-mosaic-bridge-vfilter=

# Transparency (intero)
#sout-mosaic-bridge-alpha=255

# X offset (intero)
#sout-mosaic-bridge-x=-1

# Y offset (intero)
#sout-mosaic-bridge-y=-1

[es] # Elementary stream output

# Output access method (stringa)
#sout-es-access=

# Output muxer (stringa)
#sout-es-mux=

# Output URL (stringa)
#sout-es-dst=

# Audio output access method (stringa)
#sout-es-access-audio=

# Audio output muxer (stringa)
#sout-es-mux-audio=

# Audio output URL (stringa)
#sout-es-dst-audio=

# Video output access method (stringa)
#sout-es-access-video=

# Video output muxer (stringa)
#sout-es-mux-video=

# Video output URL (stringa)
#sout-es-dst-video=

[display] # Display stream output

# Enable audio (booleano)
#sout-display-audio=1

# Enable video (booleano)
#sout-display-video=1

# Delay (ms) (intero)
#sout-display-delay=100

[delay] # Delay a stream

# Elementary Stream ID (intero)
#sout-delay-id=0

# Delay of the ES (ms) (intero)
#sout-delay-delay=0

[stream_out_chromecast] # Chromecast stream output

# ? (stringa)
#sout-chromecast-ip=

# ? (intero)
#sout-chromecast-port=8009

# ? (booleano)
#sout-chromecast-video=1

# HTTP port (intero)
#sout-chromecast-http-port=8010

# Performance warning (intero)
#sout-chromecast-show-perf-warning=1

# Enable Audio passthrough (booleano)
#sout-chromecast-audio-passthrough=0

# Conversion quality (intero)
#sout-chromecast-conversion-quality=1

[stream_out_chromaprint] # Chromaprint stream output

# Duration of the fingerprinting (intero)
#duration=90

[bridge] # Bridge stream output

# ID (intero)
#sout-bridge-out-id=0

# Destination bridge-in name (stringa)
#sout-bridge-out-in-name=default

# Delay (intero)
#sout-bridge-in-delay=0

# ID Offset (intero)
#sout-bridge-in-id-offset=8192

# Name of current instance (stringa)
#sout-bridge-in-name=default

# Fallback to placeholder stream when out of data (booleano)
#sout-bridge-in-placeholder=0

# Placeholder delay (intero)
#sout-bridge-in-placeholder-delay=200

# Wait for I frame before toggling placeholder (booleano)
#sout-bridge-in-placeholder-switch-on-iframe=1

[prefetch] # Stream prefetch filter

# Buffer size (intero)
#prefetch-buffer-size=16384

# Read size (intero)
#prefetch-read-size=16777216

# Seek threshold (intero)
#prefetch-seek-threshold=16384

[subsdelay] # Subtitle delay

# Delay calculation mode (intero)
#subsdelay-mode=1

# Calculation factor (virgola mobile)
#subsdelay-factor=2.000000

# Maximum overlapping subtitles (intero)
#subsdelay-overlap=3

# Minimum alpha value (intero)
#subsdelay-min-alpha=70

# Interval between two disappearances (intero)
#subsdelay-min-stops=1000

# Interval between appearance and disappearance (intero)
#subsdelay-min-start-stop=1000

# Interval between disappearance and appearance (intero)
#subsdelay-min-stop-start=1000

[rss] # RSS and Atom feed display

# Feed URLs (stringa)
#rss-urls=

# X offset (intero)
#rss-x=0

# Y offset (intero)
#rss-y=0

# Text position (intero)
#rss-position=-1

# Opacity (intero)
#rss-opacity=255

# Color (intero)
#rss-color=16777215

# Font size, pixels (intero)
#rss-size=0

# Speed of feeds (intero)
#rss-speed=100000

# Max length (intero)
#rss-length=60

# Refresh time (intero)
#rss-ttl=1800

# Feed images (booleano)
#rss-images=1

# Title display mode (intero)
#rss-title=-1

[remoteosd] # Remote-OSD over VNC

# VNC Host (stringa)
#rmtosd-host=myvdr

# VNC Port (intero)
#rmtosd-port=20001

# VNC Password (stringa)
#rmtosd-password=

# VNC poll interval (intero)
#rmtosd-update=1000

# VNC polling (booleano)
#rmtosd-vnc-polling=0

# Mouse events (booleano)
#rmtosd-mouse-events=0

# Key events (booleano)
#rmtosd-key-events=0

# Alpha transparency value (default 255) (intero)
#rmtosd-alpha=255

[mosaic] # Mosaic video sub source

# Transparency (intero)
#mosaic-alpha=255

# Height (intero)
#mosaic-height=100

# Width (intero)
#mosaic-width=100

# Mosaic alignment (intero)
#mosaic-align=5

# Top left corner X coordinate (intero)
#mosaic-xoffset=0

# Top left corner Y coordinate (intero)
#mosaic-yoffset=0

# Border width (intero)
#mosaic-borderw=0

# Border height (intero)
#mosaic-borderh=0

# Positioning method (intero)
#mosaic-position=0

# Number of rows (intero)
#mosaic-rows=2

# Number of columns (intero)
#mosaic-cols=2

# Keep aspect ratio (booleano)
#mosaic-keep-aspect-ratio=0

# Keep original size (booleano)
#mosaic-keep-picture=0

# Elements order (stringa)
#mosaic-order=

# Offsets in order (stringa)
#mosaic-offsets=

# Delay (intero)
#mosaic-delay=0

[marq] # Marquee display

# Text (stringa)
#marq-marquee=VLC

# Text file (stringa)
#marq-file=

# X offset (intero)
#marq-x=0

# Y offset (intero)
#marq-y=0

# Marquee position (intero)
#marq-position=-1

# Opacity (intero)
#marq-opacity=255

# Color (intero)
#marq-color=16777215

# Font size, pixels (intero)
#marq-size=0

# Timeout (intero)
#marq-timeout=0

# Refresh period in ms (intero)
#marq-refresh=1000

[logo] # Logo sub source

# Logo filenames (stringa)
#logo-file=

# X coordinate (intero)
#logo-x=-1

# Y coordinate (intero)
#logo-y=-1

# Logo individual image time in ms (intero)
#logo-delay=1000

# Logo animation # of loops (intero)
#logo-repeat=-1

# Opacity of the logo (intero)
#logo-opacity=255

# Logo position (intero)
#logo-position=-1

[audiobargraph_v] # Audio Bar Graph Video sub source

# X coordinate (intero)
#audiobargraph_v-x=0

# Y coordinate (intero)
#audiobargraph_v-y=0

# Transparency of the bargraph (intero)
#audiobargraph_v-transparency=255

# Bargraph position (intero)
#audiobargraph_v-position=-1

# Bar width in pixel (intero)
#audiobargraph_v-barWidth=10

# Bar Height in pixel (intero)
#audiobargraph_v-barHeight=400

[upnp] # Universal Plug'n'Play

# Custom SAT>IP channel list URL (stringa)
#satip-channellist-url=

[sap] # Network streams (SAP)

# SAP multicast address (stringa)
#sap-addr=

# SAP timeout (seconds) (intero)
#sap-timeout=1800

# Try to parse the announce (booleano)
#sap-parse=1

# SAP Strict mode (booleano)
#sap-strict=0

[podcast] # Podcasts

# Podcast URLs list (stringa)
#podcast-urls=

[mpegvideo] # MPEG-I/II video packetizer

# Sync on Intra Frame (booleano)
#packetizer-mpegvideo-sync-iframe=0

[mux_ts] # TS muxer (libdvbpsi)

# Digital TV Standard (stringa)
#sout-ts-standard=dvb

# Video PID (intero)
#sout-ts-pid-video=100

# Audio PID (intero)
#sout-ts-pid-audio=200

# SPU PID (intero)
#sout-ts-pid-spu=300

# PMT PID (intero)
#sout-ts-pid-pmt=32

# TS ID (intero)
#sout-ts-tsid=0

# NET ID (intero)
#sout-ts-netid=0

# PMT Program numbers (stringa)
#sout-ts-program-pmt=

# Set PID to ID of ES (booleano)
#sout-ts-es-id-pid=0

# Mux PMT (requires --sout-ts-es-id-pid) (stringa)
#sout-ts-muxpmt=

# SDT Descriptors (requires --sout-ts-es-id-pid) (stringa)
#sout-ts-sdtdesc=

# Data alignment (booleano)
#sout-ts-alignment=1

# Shaping delay (ms) (intero)
#sout-ts-shaping=200

# Use keyframes (booleano)
#sout-ts-use-key-frames=0

# PCR interval (ms) (intero)
#sout-ts-pcr=70

# Minimum B (deprecated) (intero)
#sout-ts-bmin=0

# Maximum B (deprecated) (intero)
#sout-ts-bmax=0

# DTS delay (ms) (intero)
#sout-ts-dts-delay=400

# Crypt audio (booleano)
#sout-ts-crypt-audio=1

# Crypt video (booleano)
#sout-ts-crypt-video=1

# CSA Key (stringa)
#sout-ts-csa-ck=

# Second CSA Key (stringa)
#sout-ts-csa2-ck=

# CSA Key in use (stringa)
#sout-ts-csa-use=1

# Packet size in bytes to encrypt (intero)
#sout-ts-csa-pkt=188

[ps] # PS muxer

# DTS delay (ms) (intero)
#sout-ps-dts-delay=200

# PES maximum size (intero)
#sout-ps-pes-max-size=65500

[mux_ogg] # Ogg/OGM muxer

# Index interval (intero)
#sout-ogg-indexintvl=1000

# Index size ratio (virgola mobile)
#sout-ogg-indexratio=1.000000

[mp4] # MP4/MOV muxer

# Create "Fast Start" files (booleano)
#sout-mp4-faststart=1

[avi] # AVI muxer

# Artist (stringa)
#sout-avi-artist=

# Date (stringa)
#sout-avi-date=

# Genre (stringa)
#sout-avi-genre=

# Copyright (stringa)
#sout-avi-copyright=

# Comment (stringa)
#sout-avi-comment=

# Name (stringa)
#sout-avi-name=

# Subject (stringa)
#sout-avi-subject=

# Encoder (stringa)
#sout-avi-encoder=VLC Media Player - 3.0.23 Vetinari

# Keywords (stringa)
#sout-avi-keywords=

[asf] # ASF muxer

# Title (stringa)
#sout-asf-title=

# Author (stringa)
#sout-asf-author=

# Copyright (stringa)
#sout-asf-copyright=

# Comment (stringa)
#sout-asf-comment=

# Rating (stringa)
#sout-asf-rating=

# Packet Size (intero)
#sout-asf-packet-size=4096

# Bitrate override (intero)
#sout-asf-bitrate-override=0

[rtsp] # Legacy RTSP VoD server

# MUX for RAW RTSP transport (stringa)
#rtsp-raw-mux=ts

# Maximum number of connections (intero)
#rtsp-throttle-users=0

# Sets the timeout option in the RTSP session string (intero)
#rtsp-session-timeout=5

[logger] # File logging

[gnutls] # GNU TLS transport layer security

# Use system trust database (booleano)
#gnutls-system-trust=1

# Trust directory (stringa)
#gnutls-dir-trust=

# TLS cipher priorities (stringa)
#gnutls-priorities=NORMAL

[audioscrobbler] # Submission of played songs to last.fm

# Username (stringa)
#lastfm-username=

# Password (stringa)
#lastfm-password=

# Scrobbler URL (stringa)
#scrobbler-url=post.audioscrobbler.com

[folder] # Folder meta data

# Album art filename (stringa)
#album-art-filename=

[lua] # Lua interpreter

# Lua interface (stringa)
#lua-intf=dummy

# Lua interface configuration (stringa)
#lua-config=

# Password (stringa)
#http-password=

# Source directory (stringa)
#http-src=

# Directory index (booleano)
#http-index=0

# TCP command input (stringa)
#rc-host=

# CLI input (stringa)
#cli-host=

# Host (stringa)
#telnet-host=localhost

# Port (intero)
#telnet-port=4212

# Password (stringa)
#telnet-password=

[file] # File logger

# Log to file (booleano)
#file-logging=0

# Log filename (stringa)
#logfile=

# Log format (stringa)
#logmode=text

# Verbosity (intero)
#log-verbose=-1

[console] # Console logger

[file] # Secrets are stored on a file without any encryption

# ? (stringa)
#keystore-file=

[skins2] # Skinnable Interface

# Skin to use (stringa)
#skins2-last=

# Config of last used skin (stringa)
#skins2-config=

# Systray icon (booleano)
#skins2-systray=1

# Show VLC on the taskbar (booleano)
#skins2-taskbar=1

# Enable transparency effects (booleano)
#skins2-transparency=0

# Use a skinned playlist (booleano)
#skinned-playlist=1

# Display video in a skinned window if any (booleano)
#skinned-video=1

[qt] # Qt interface

# Start in minimal view (without menus) (booleano)
#qt-minimal-view=0

# Systray icon (booleano)
#qt-system-tray=1

# Show notification popup on track change (intero)
#qt-notification=1

# Start VLC with only a systray icon (booleano)
#qt-start-minimized=0

# Pause the video playback when minimized (booleano)
#qt-pause-minimized=0

# Windows opacity between 0.1 and 1 (virgola mobile)
#qt-opacity=1.000000

# Fullscreen controller opacity between 0.1 and 1 (virgola mobile)
#qt-fs-opacity=0.800000

# Resize interface to the native video size (booleano)
#qt-video-autoresize=1

# Show playing item name in window title (booleano)
#qt-name-in-title=1

# Show a controller in fullscreen mode (booleano)
#qt-fs-controller=1

# Save the recently played items in the menu (booleano)
#qt-recentplay=1

# List of words separated by | to filter (stringa)
#qt-recentplay-filter=

# Continue playback? (intero)
#qt-continue=1

# Enable Dark Mode (booleano)
#qt-dark-palette=0

# Activate the updates availability notification (booleano)
#qt-updates-notif=1

# Number of days between two update checks (intero)
#qt-updates-days=3

# Ignore keyboard volume buttons. (booleano)
#qt-disable-volume-keys=1

# Embed the file browser in open dialog (booleano)
#qt-embedded-open=0

# Show advanced preferences over simple ones (booleano)
#qt-advanced-pref=0

# Show unimportant error and warnings dialogs (booleano)
#qt-error-dialogs=1

# Define the colors of the volume slider (stringa)
#qt-slider-colours=153;210;153;20;210;20;255;199;15;245;39;29

# Ask for network policy at start (booleano)
qt-privacy-ask=0

# Define which screen fullscreen goes (intero)
#qt-fullscreen-screennumber=-1

# Load extensions on startup (booleano)
#qt-autoload-extensions=1

# Display background cone or art (booleano)
#qt-bgcone=1

# Expanding background cone or art (booleano)
#qt-bgcone-expands=0

# Allow automatic icon changes (booleano)
#qt-icon-change=1

# Maximum Volume displayed (intero)
#qt-max-volume=125

# Fullscreen controller mouse sensitivity (intero)
#qt-fs-sensitivity=3

# When to raise the interface (intero)
#qt-auto-raise=1

[vc1] # VC1 video demuxer

# Frames per Second (virgola mobile)
#vc1-fps=25.000000

[ts] # MPEG Transport Stream demuxer

# Digital TV Standard (stringa)
#ts-standard=auto

# Extra PMT (stringa)
#ts-extra-pmt=

# Trust in-stream PCR (booleano)
#ts-trust-pcr=1

# Set id of ES to PID (booleano)
#ts-es-id-pid=1

# CSA Key (stringa)
#ts-csa-ck=

# Second CSA Key (stringa)
#ts-csa2-ck=

# Packet size in bytes to decrypt (intero)
#ts-csa-pkt=188

# Separate sub-streams (booleano)
#ts-split-es=1

# Seek based on percent not time (booleano)
#ts-seek-percent=0

# Check packets continuity counter (booleano)
#ts-cc-check=1

# Only create ES on program sending data (booleano)
#ts-pmtfix-waitdata=1

# Try to generate PAT/PMT if missing (booleano)
#ts-patfix=1

# Try to fix too early PCR (or late DTS) (booleano)
#ts-pcr-offsetfix=1

[subtitle] # Text subtitle parser

# Frames per Second (virgola mobile)
#sub-fps=0.000000

# Subtitle delay (intero)
#sub-delay=0

# Subtitle format (stringa)
#sub-type=auto

# Subtitle description (stringa)
#sub-description=

[rawvid] # Raw video demuxer

# Frames per Second (stringa)
#rawvid-fps=

# Width (intero)
#rawvid-width=0

# Height (intero)
#rawvid-height=0

# Force chroma (Use carefully) (stringa)
#rawvid-chroma=

# Aspect ratio (stringa)
#rawvid-aspect-ratio=

[rawdv] # DV (Digital Video) demuxer

# Hurry up (booleano)
#rawdv-hurry-up=0

[rawaud] # Raw audio demuxer

# Audio channels (intero)
#rawaud-channels=2

# Audio samplerate (Hz) (intero)
#rawaud-samplerate=48000

# FOURCC code of raw input format (stringa)
#rawaud-fourcc=s16l

# Forces the audio language (stringa)
#rawaud-lang=eng

[ps] # MPEG-PS demuxer

# Trust MPEG timestamps (booleano)
#ps-trust-timestamps=1

[playlist] # Playlist

# Skip ads (booleano)
#playlist-skip-ads=1

# Show shoutcast adult content (booleano)
#shoutcast-show-adult=0

[mp4] # MP4 stream demuxer

# M4A audio only (booleano)
#mp4-m4a-audioonly=0

[mod] # MOD demuxer (libmodplug)

# Noise reduction (booleano)
#mod-noisereduction=1

# Reverb (booleano)
#mod-reverb=0

# Reverberation level (intero)
#mod-reverb-level=0

# Reverberation delay (intero)
#mod-reverb-delay=40

# Mega bass (booleano)
#mod-megabass=0

# Mega bass level (intero)
#mod-megabass-level=0

# Mega bass cutoff (intero)
#mod-megabass-range=10

# Surround (booleano)
#mod-surround=0

# Surround level (intero)
#mod-surround-level=0

# Surround delay (ms) (intero)
#mod-surround-delay=5

[mkv] # Matroska stream demuxer

# Respect ordered chapters (booleano)
#mkv-use-ordered-chapters=1

# Chapter codecs (booleano)
#mkv-use-chapter-codec=1

# Preload MKV files in the same directory (booleano)
#mkv-preload-local-dir=1

# Seek based on percent not time (booleano)
#mkv-seek-percent=0

# Dummy Elements (booleano)
#mkv-use-dummy=0

# Preload clusters (booleano)
#mkv-preload-clusters=0

[mjpeg] # M-JPEG camera demuxer

# Frames per Second (virgola mobile)
#mjpeg-fps=0.000000

[image] # Image demuxer

# ES ID (intero)
#image-id=-1

# Group (intero)
#image-group=0

# Decode (booleano)
#image-decode=1

# Forced chroma (stringa)
#image-chroma=

# Duration in seconds (virgola mobile)
#image-duration=10.000000

# Frame rate (stringa)
#image-fps=10/1

# Real-time (booleano)
#image-realtime=0

[h26x] # H264 video demuxer

# Frames per Second (virgola mobile)
#h264-fps=0.000000

# Frames per Second (virgola mobile)
#hevc-fps=0.000000

[es] # MPEG-I/II/4 / A52 / DTS / MLP audio

# Frames per Second (virgola mobile)
#es-fps=25.000000

[diracsys] # Dirac video demuxer

# Value to adjust dts by (intero)
#dirac-dts-offset=0

[demuxdump] # File dumper

# Dump module (stringa)
#demuxdump-access=file

# Dump filename (stringa)
#demuxdump-file=stream-demux.dump

# Append to existing file (booleano)
#demuxdump-append=0

[avi] # AVI demuxer

# Force interleaved method (booleano)
#avi-interleaved=0

# Force index creation (intero)
#avi-index=0

[adaptive] # Unified adaptive streaming for DASH/HLS

# Adaptive Logic (stringa)
#adaptive-logic=

# Maximum device width (intero)
#adaptive-maxwidth=0

# Maximum device height (intero)
#adaptive-maxheight=0

# Fixed Bandwidth in KiB/s (intero)
#adaptive-bw=250

# Use regular HTTP modules (booleano)
#adaptive-use-access=0

# Live Playback delay (ms) (intero)
#adaptive-livedelay=15000

# Max buffering (ms) (intero)
#adaptive-maxbuffer=30000

# Low latency (intero)
#adaptive-lowlatency=-1

[d3d9_filters] # Direct3D9 adjust filter

# Image contrast (0-2) (virgola mobile)
#contrast=1.000000

# Image brightness (0-2) (virgola mobile)
#brightness=1.000000

# Image hue (0-360) (virgola mobile)
#hue=0.000000

# Image saturation (0-3) (virgola mobile)
#saturation=1.000000

# Image gamma (0-10) (virgola mobile)
#gamma=1.000000

# Brightness threshold (booleano)
#brightness-threshold=0

[d3d11_filters] # Direct3D11 adjust filter

# Image contrast (0-2) (virgola mobile)
#contrast=1.000000

# Image brightness (0-2) (virgola mobile)
#brightness=1.000000

# Image hue (0-360) (virgola mobile)
#hue=0.000000

# Image saturation (0-3) (virgola mobile)
#saturation=1.000000

# Image gamma (0-10) (virgola mobile)
#gamma=1.000000

# Brightness threshold (booleano)
#brightness-threshold=0

[oldrc] # Remote control interface

# Show stream position (booleano)
#rc-show-pos=0

# Do not open a DOS command box interface (booleano)
#rc-quiet=0

# TCP command input (stringa)
#rc-host=

[ntservice] # Windows Service interface

# Install Windows Service (booleano)
#ntservice-install=0

# Uninstall Windows Service (booleano)
#ntservice-uninstall=0

# Display name of the Service (stringa)
#ntservice-name=VLC media player

# Configuration options (stringa)
#ntservice-options=

# Extra interface modules (stringa)
#ntservice-extraintf=

[netsync] # Network synchronization

# Network master clock (booleano)
#netsync-master=0

# Master server IP address (stringa)
#netsync-master-ip=

# UDP timeout (in ms) (intero)
#netsync-timeout=500

[gestures] # Mouse gestures control interface

# Motion threshold (10-100) (intero)
#gestures-threshold=30

# Trigger button (stringa)
#gestures-button=left

[dummy] # Dummy interface

# Do not open a DOS command box interface (booleano)
#dummy-quiet=0

[zvbi] # VBI and Teletext decoder

# Teletext page (intero)
#vbi-page=100

# Opacity (booleano)
#vbi-opaque=0

# Teletext alignment (intero)
#vbi-position=8

# Teletext text subtitles (booleano)
#vbi-text=0

# Presentation Level (intero)
#vbi-level=3

[x264] # H.264/MPEG-4 Part 10/AVC encoder (x264)

# Maximum GOP size (intero)
#sout-x264-keyint=250

# Minimum GOP size (intero)
#sout-x264-min-keyint=25

# Use recovery points to close GOPs (booleano)
#sout-x264-opengop=0

# Enable compatibility hacks for Blu-ray support (booleano)
#sout-x264-bluray-compat=0

# Extra I-frames aggressivity (intero)
#sout-x264-scenecut=40

# B-frames between I and P (intero)
#sout-x264-bframes=3

# Adaptive B-frame decision (intero)
#sout-x264-b-adapt=1

# Influence (bias) B-frames usage (intero)
#sout-x264-b-bias=0

# Keep some B-frames as references (stringa)
#sout-x264-bpyramid=normal

# CABAC (booleano)
#sout-x264-cabac=1

# Use fullrange instead of TV colorrange (booleano)
#sout-x264-fullrange=0

# Number of reference frames (intero)
#sout-x264-ref=3

# Skip loop filter (booleano)
#sout-x264-nf=0

# Loop filter AlphaC0 and Beta parameters alpha:beta (stringa)
#sout-x264-deblock=0:0

# Strength of psychovisual optimization, default is "1.0:0.0" (stringa)
#sout-x264-psy-rd=1.0:0.0

# Use Psy-optimizations (booleano)
#sout-x264-psy=1

# H.264 level (stringa)
#sout-x264-level=0

# H.264 profile (stringa)
#sout-x264-profile=high

# Interlaced mode (booleano)
#sout-x264-interlaced=0

# Frame packing (intero)
#sout-x264-frame-packing=-1

# Force number of slices per frame (intero)
#sout-x264-slices=0

# Limit the size of each slice in bytes (intero)
#sout-x264-slice-max-size=0

# Limit the size of each slice in macroblocks (intero)
#sout-x264-slice-max-mbs=0

# HRD-timing information (stringa)
#sout-x264-hrd=none

# Set QP (intero)
#sout-x264-qp=-1

# Quality-based VBR (intero)
#sout-x264-crf=23

# Min QP (intero)
#sout-x264-qpmin=10

# Max QP (intero)
#sout-x264-qpmax=51

# Max QP step (intero)
#sout-x264-qpstep=4

# Average bitrate tolerance (virgola mobile)
#sout-x264-ratetol=1.000000

# Max local bitrate (intero)
#sout-x264-vbv-maxrate=0

# VBV buffer (intero)
#sout-x264-vbv-bufsize=0

# Initial VBV buffer occupancy (virgola mobile)
#sout-x264-vbv-init=0.900000

# QP factor between I and P (virgola mobile)
#sout-x264-ipratio=1.400000

# QP factor between P and B (virgola mobile)
#sout-x264-pbratio=1.300000

# QP difference between chroma and luma (intero)
#sout-x264-chroma-qp-offset=0

# Multipass ratecontrol (intero)
#sout-x264-pass=0

# QP curve compression (virgola mobile)
#sout-x264-qcomp=0.600000

# Reduce fluctuations in QP (virgola mobile)
#sout-x264-cplxblur=20.000000

# Reduce fluctuations in QP (virgola mobile)
#sout-x264-qblur=0.500000

# How AQ distributes bits (intero)
#sout-x264-aq-mode=1

# Strength of AQ (virgola mobile)
#sout-x264-aq-strength=1.000000

# Partitions to consider (stringa)
#sout-x264-partitions=normal

# Direct MV prediction mode (stringa)
#sout-x264-direct=spatial

# Direct prediction size (intero)
#sout-x264-direct-8x8=1

# Weighted prediction for B-frames (booleano)
#sout-x264-weightb=1

# Weighted prediction for P-frames (intero)
#sout-x264-weightp=2

# Integer pixel motion estimation method (stringa)
#sout-x264-me=hex

# Maximum motion vector search range (intero)
#sout-x264-merange=16

# Maximum motion vector length (intero)
#sout-x264-mvrange=-1

# Minimum buffer space between threads (intero)
#sout-x264-mvrange-thread=-1

# Subpixel motion estimation and partition decision quality (intero)
#sout-x264-subme=7

# Decide references on a per partition basis (booleano)
#sout-x264-mixed-refs=1

# Chroma in motion estimation (booleano)
#sout-x264-chroma-me=1

# Adaptive spatial transform size (booleano)
#sout-x264-8x8dct=1

# Trellis RD quantization (intero)
#sout-x264-trellis=1

# Framecount to use on frametype lookahead (intero)
#sout-x264-lookahead=40

# Use Periodic Intra Refresh (booleano)
#sout-x264-intra-refresh=0

# Use mb-tree ratecontrol (booleano)
#sout-x264-mbtree=1

# Early SKIP detection on P-frames (booleano)
#sout-x264-fast-pskip=1

# Coefficient thresholding on P-frames (booleano)
#sout-x264-dct-decimate=1

# Noise reduction (intero)
#sout-x264-nr=0

# Inter luma quantization deadzone (intero)
#sout-x264-deadzone-inter=21

# Intra luma quantization deadzone (intero)
#sout-x264-deadzone-intra=11

# Non-deterministic optimizations when threaded (booleano)
#sout-x264-non-deterministic=0

# CPU optimizations (booleano)
#sout-x264-asm=1

# PSNR computation (booleano)
#sout-x264-psnr=0

# SSIM computation (booleano)
#sout-x264-ssim=0

# Quiet mode (booleano)
#sout-x264-quiet=0

# SPS and PPS id numbers (intero)
#sout-x264-sps-id=0

# Access unit delimiters (booleano)
#sout-x264-aud=0

# Statistics (booleano)
#sout-x264-verbose=0

# Filename for 2 pass stats file (stringa)
#sout-x264-stats=x264_2pass.log

# Default preset setting used (stringa)
#sout-x264-preset=

# Default tune setting used (stringa)
#sout-x264-tune=

# x264 advanced options (stringa)
#sout-x264-options=

[x26410b] # H.264/MPEG-4 Part 10/AVC encoder (x264 10-bit)

# Maximum GOP size (intero)
#sout-x26410b-keyint=250

# Minimum GOP size (intero)
#sout-x26410b-min-keyint=25

# Use recovery points to close GOPs (booleano)
#sout-x26410b-opengop=0

# Enable compatibility hacks for Blu-ray support (booleano)
#sout-x26410b-bluray-compat=0

# Extra I-frames aggressivity (intero)
#sout-x26410b-scenecut=40

# B-frames between I and P (intero)
#sout-x26410b-bframes=3

# Adaptive B-frame decision (intero)
#sout-x26410b-b-adapt=1

# Influence (bias) B-frames usage (intero)
#sout-x26410b-b-bias=0

# Keep some B-frames as references (stringa)
#sout-x26410b-bpyramid=normal

# CABAC (booleano)
#sout-x26410b-cabac=1

# Use fullrange instead of TV colorrange (booleano)
#sout-x26410b-fullrange=0

# Number of reference frames (intero)
#sout-x26410b-ref=3

# Skip loop filter (booleano)
#sout-x26410b-nf=0

# Loop filter AlphaC0 and Beta parameters alpha:beta (stringa)
#sout-x26410b-deblock=0:0

# Strength of psychovisual optimization, default is "1.0:0.0" (stringa)
#sout-x26410b-psy-rd=1.0:0.0

# Use Psy-optimizations (booleano)
#sout-x26410b-psy=1

# H.264 level (stringa)
#sout-x26410b-level=0

# H.264 profile (stringa)
#sout-x26410b-profile=high

# Interlaced mode (booleano)
#sout-x26410b-interlaced=0

# Frame packing (intero)
#sout-x26410b-frame-packing=-1

# Force number of slices per frame (intero)
#sout-x26410b-slices=0

# Limit the size of each slice in bytes (intero)
#sout-x26410b-slice-max-size=0

# Limit the size of each slice in macroblocks (intero)
#sout-x26410b-slice-max-mbs=0

# HRD-timing information (stringa)
#sout-x26410b-hrd=none

# Set QP (intero)
#sout-x26410b-qp=-1

# Quality-based VBR (intero)
#sout-x26410b-crf=23

# Min QP (intero)
#sout-x26410b-qpmin=10

# Max QP (intero)
#sout-x26410b-qpmax=51

# Max QP step (intero)
#sout-x26410b-qpstep=4

# Average bitrate tolerance (virgola mobile)
#sout-x26410b-ratetol=1.000000

# Max local bitrate (intero)
#sout-x26410b-vbv-maxrate=0

# VBV buffer (intero)
#sout-x26410b-vbv-bufsize=0

# Initial VBV buffer occupancy (virgola mobile)
#sout-x26410b-vbv-init=0.900000

# QP factor between I and P (virgola mobile)
#sout-x26410b-ipratio=1.400000

# QP factor between P and B (virgola mobile)
#sout-x26410b-pbratio=1.300000

# QP difference between chroma and luma (intero)
#sout-x26410b-chroma-qp-offset=0

# Multipass ratecontrol (intero)
#sout-x26410b-pass=0

# QP curve compression (virgola mobile)
#sout-x26410b-qcomp=0.600000

# Reduce fluctuations in QP (virgola mobile)
#sout-x26410b-cplxblur=20.000000

# Reduce fluctuations in QP (virgola mobile)
#sout-x26410b-qblur=0.500000

# How AQ distributes bits (intero)
#sout-x26410b-aq-mode=1

# Strength of AQ (virgola mobile)
#sout-x26410b-aq-strength=1.000000

# Partitions to consider (stringa)
#sout-x26410b-partitions=normal

# Direct MV prediction mode (stringa)
#sout-x26410b-direct=spatial

# Direct prediction size (intero)
#sout-x26410b-direct-8x8=1

# Weighted prediction for B-frames (booleano)
#sout-x26410b-weightb=1

# Weighted prediction for P-frames (intero)
#sout-x26410b-weightp=2

# Integer pixel motion estimation method (stringa)
#sout-x26410b-me=hex

# Maximum motion vector search range (intero)
#sout-x26410b-merange=16

# Maximum motion vector length (intero)
#sout-x26410b-mvrange=-1

# Minimum buffer space between threads (intero)
#sout-x26410b-mvrange-thread=-1

# Subpixel motion estimation and partition decision quality (intero)
#sout-x26410b-subme=7

# Decide references on a per partition basis (booleano)
#sout-x26410b-mixed-refs=1

# Chroma in motion estimation (booleano)
#sout-x26410b-chroma-me=1

# Adaptive spatial transform size (booleano)
#sout-x26410b-8x8dct=1

# Trellis RD quantization (intero)
#sout-x26410b-trellis=1

# Framecount to use on frametype lookahead (intero)
#sout-x26410b-lookahead=40

# Use Periodic Intra Refresh (booleano)
#sout-x26410b-intra-refresh=0

# Use mb-tree ratecontrol (booleano)
#sout-x26410b-mbtree=1

# Early SKIP detection on P-frames (booleano)
#sout-x26410b-fast-pskip=1

# Coefficient thresholding on P-frames (booleano)
#sout-x26410b-dct-decimate=1

# Noise reduction (intero)
#sout-x26410b-nr=0

# Inter luma quantization deadzone (intero)
#sout-x26410b-deadzone-inter=21

# Intra luma quantization deadzone (intero)
#sout-x26410b-deadzone-intra=11

# Non-deterministic optimizations when threaded (booleano)
#sout-x26410b-non-deterministic=0

# CPU optimizations (booleano)
#sout-x26410b-asm=1

# PSNR computation (booleano)
#sout-x26410b-psnr=0

# SSIM computation (booleano)
#sout-x26410b-ssim=0

# Quiet mode (booleano)
#sout-x26410b-quiet=0

# SPS and PPS id numbers (intero)
#sout-x26410b-sps-id=0

# Access unit delimiters (booleano)
#sout-x26410b-aud=0

# Statistics (booleano)
#sout-x26410b-verbose=0

# Filename for 2 pass stats file (stringa)
#sout-x26410b-stats=x264_2pass.log

# Default preset setting used (stringa)
#sout-x26410b-preset=

# Default tune setting used (stringa)
#sout-x26410b-tune=

# x264 advanced options (stringa)
#sout-x26410b-options=

[vpx] # WebM video decoder

# Quality mode (intero)
#sout-vpx-quality-mode=1000000

[vorbis] # Vorbis audio decoder

# Encoding quality (intero)
#sout-vorbis-quality=0

# Maximum encoding bitrate (intero)
#sout-vorbis-max-bitrate=0

# Minimum encoding bitrate (intero)
#sout-vorbis-min-bitrate=0

# CBR encoding (booleano)
#sout-vorbis-cbr=0

[twolame] # Libtwolame audio encoder

# Encoding quality (virgola mobile)
#sout-twolame-quality=0.000000

# Stereo mode (intero)
#sout-twolame-mode=0

# VBR mode (booleano)
#sout-twolame-vbr=0

# Psycho-acoustic model (intero)
#sout-twolame-psy=3

[ttml] # TTML subtitles decoder

# Subtitle justification (intero)
#ttml-align=0

[theora] # Theora video decoder

# Post processing quality (intero)
#theora-postproc=-1

# Encoding quality (intero)
#sout-theora-quality=2

[svcdsub] # Philips OGT (SVCD subtitle) decoder

[subsusf] # USF subtitles decoder

# Formatted Subtitles (booleano)
#subsdec-formatted=1

[subsdec] # Text subtitle decoder

# Subtitle justification (intero)
#subsdec-align=-1

# Subtitle text encoding (stringa)
#subsdec-encoding=

# UTF-8 subtitle autodetection (booleano)
#subsdec-autodetect-utf8=1

[spudec] # DVD subtitles decoder

# Disable DVD subtitle transparency (booleano)
#dvdsub-transparency=0

[speex] # Speex audio decoder

# Mode (intero)
#sout-speex-mode=0

# Encoding complexity (intero)
#sout-speex-complexity=3

# CBR encoding (booleano)
#sout-speex-cbr=0

# Encoding quality (virgola mobile)
#sout-speex-quality=8.000000

# Maximal bitrate (intero)
#sout-speex-max-bitrate=0

# Voice activity detection (booleano)
#sout-speex-vad=1

# Discontinuous Transmission (booleano)
#sout-speex-dtx=0

[schroedinger] # Dirac video decoder using libschroedinger

# Rate control method (stringa)
#sout-schro-rate-control=

# Constant quality factor (virgola mobile)
#sout-schro-quality=-1.000000

# Noise Threshold (virgola mobile)
#sout-schro-noise-threshold=-1.000000

# CBR bitrate (kbps) (intero)
#sout-schro-bitrate=-1

# Maximum bitrate (kbps) (intero)
#sout-schro-max-bitrate=-1

# Minimum bitrate (kbps) (intero)
#sout-schro-min-bitrate=-1

# GOP structure (stringa)
#sout-schro-gop-structure=

# GOP length (intero)
#sout-schro-gop-length=-1

# Chroma format (stringa)
#sout-schro-chroma-fmt=420

# Picture coding mode (stringa)
#sout-schro-coding-mode=auto

# Motion Vector precision (stringa)
#sout-schro-mv-precision=

# Size of motion compensation blocks (stringa)
#sout-schro-motion-block-size=

# Overlap of motion compensation blocks (stringa)
#sout-schro-motion-block-overlap=

# Three component motion estimation (intero)
#sout-schro-me-combined=-1

# Enable hierarchical Motion Estimation (intero)
#sout-schro-enable-hierarchical-me=-1

# Number of levels of downsampling (intero)
#sout-schro-downsample-levels=-1

# Enable Global Motion Estimation (intero)
#sout-schro-enable-global-me=-1

# Enable Phase Correlation Estimation (intero)
#sout-schro-enable-phasecorr-me=-1

# Intra picture DWT filter (stringa)
#sout-schro-intra-wavelet=

# Inter picture DWT filter (stringa)
#sout-schro-inter-wavelet=

# Number of DWT iterations (intero)
#sout-schro-transform-depth=-1

# Enable multiple quantizers (intero)
#sout-schro-enable-multiquant=-1

# Size of code blocks in each subband (stringa)
#sout-schro-codeblock-size=

# Prefilter (stringa)
#sout-schro-filtering=

# Amount of prefiltering (virgola mobile)
#sout-schro-filter-value=-1.000000

# Enable Scene Change Detection (intero)
#sout-schro-enable-scd=-1

# perceptual weighting method (stringa)
#sout-schro-perceptual-weighting=

# perceptual distance (virgola mobile)
#sout-schro-perceptual-distance=-1.000000

# Disable arithmetic coding (intero)
#sout-schro-enable-noarith=-1

# Horizontal slices per frame (intero)
#sout-schro-horiz-slices=-1

# Vertical slices per frame (intero)
#sout-schro-vert-slices=-1

# Force Profile (stringa)
#sout-schro-force-profile=

[qsv] # Intel QuickSync Video encoder for MPEG4-Part10/MPEG2 (aka H.264/H.262)

# Enable software mode (booleano)
#sout-qsv-software=0

# Codec Profile (stringa)
#sout-qsv-h264-profile=unspecified

# Codec Level (stringa)
#sout-qsv-h264-level=unspecified

# Codec Profile (stringa)
#sout-qsv-mpeg2-profile=unspecified

# Codec Level (stringa)
#sout-qsv-mpeg2-level=unspecified

# Group of Picture size (intero)
#sout-qsv-gop-size=32

# Group of Picture Reference Distance (intero)
#sout-qsv-gop-refdist=4

# IDR interval (intero)
#sout-qsv-idr-interval=0

# Target Usage (stringa)
#sout-qsv-target-usage=quality

# Rate Control Method (stringa)
#sout-qsv-rc-method=vbr

# Quantization parameter (intero)
#sout-qsv-qp=0

# Quantization parameter for I-frames (intero)
#sout-qsv-qpi=0

# Quantization parameter for P-frames (intero)
#sout-qsv-qpp=0

# Quantization parameter for B-frames (intero)
#sout-qsv-qpb=0

# Maximum Bitrate (intero)
#sout-qsv-bitrate-max=0

# Accuracy of RateControl (intero)
#sout-qsv-accuracy=0

# Convergence time of 'avbr' RateControl (intero)
#sout-qsv-convergence=0

# Number of slices per frame (intero)
#sout-qsv-num-slice=0

# Number of reference frames (intero)
#sout-qsv-num-ref-frame=0

# Number of parallel operations (intero)
#sout-qsv-async-depth=4

[libass] # Subtitle renderers using libass

# Additional fonts directory (stringa)
#ssa-fontsdir=

[kate] # Kate overlay decoder

# Formatted Subtitles (booleano)
#kate-formatted=1

[jpeg] # JPEG image decoder

# Quality level (intero)
#sout-jpeg-quality=95

[fluidsynth] # FluidSynth MIDI synthesizer

# SoundFont file (stringa)
#soundfont=

# Chorus (booleano)
#synth-chorus=1

# Synthesis gain (virgola mobile)
#synth-gain=0.500000

# Polyphony (intero)
#synth-polyphony=256

# Reverb (booleano)
#synth-reverb=1

# Sample rate (intero)
#synth-sample-rate=44100

[dvbsub] # DVB subtitles decoder

# Subpicture position (intero)
#dvbsub-position=8

# Decoding X coordinate (intero)
#dvbsub-x=-1

# Decoding Y coordinate (intero)
#dvbsub-y=-1

# Encoding X coordinate (intero)
#sout-dvbsub-x=-1

# Encoding Y coordinate (intero)
#sout-dvbsub-y=-1

[ddummy] # Dummy decoder

# Save raw codec data (booleano)
#dummy-save-es=0

[dav1d] # Dav1d video decoder

# Frames Threads (intero)
#dav1d-thread-frames=0

# All Layers (booleano)
#dav1d-all-layers=0

[cc] # Closed Captions decoder

# Opacity (booleano)
#cc-opaque=1

[avcodec] # FFmpeg audio/video decoder

# Direct rendering (booleano)
#avcodec-dr=1

# Show corrupted frames (booleano)
#avcodec-corrupted=1

# Error resilience (intero)
#avcodec-error-resilience=1

# Workaround bugs (intero)
#avcodec-workaround-bugs=1

# Hurry up (booleano)
#avcodec-hurry-up=1

# Skip frame (default=0) (intero)
#avcodec-skip-frame=0

# Skip idct (default=0) (intero)
#avcodec-skip-idct=0

# Allow speed tricks (booleano)
#avcodec-fast=0

# Skip the loop filter for H.264 decoding (intero)
#avcodec-skiploopfilter=0

# Debug mask (intero)
#avcodec-debug=0

# Codec name (stringa)
#avcodec-codec=

# Hardware decoding (stringa)
#avcodec-hw=any

# Threads (intero)
#avcodec-threads=0

# Advanced options (stringa)
#avcodec-options=

# Codec name (stringa)
#sout-avcodec-codec=

# Quality level (stringa)
#sout-avcodec-hq=rd

# Ratio of key frames (intero)
#sout-avcodec-keyint=0

# Ratio of B frames (intero)
#sout-avcodec-bframes=0

# Hurry up (booleano)
#sout-avcodec-hurry-up=0

# Interlaced encoding (booleano)
#sout-avcodec-interlace=0

# Interlaced motion estimation (booleano)
#sout-avcodec-interlace-me=1

# Video bitrate tolerance (intero)
#sout-avcodec-vt=0

# Pre-motion estimation (booleano)
#sout-avcodec-pre-me=0

# Rate control buffer size (intero)
#sout-avcodec-rc-buffer-size=0

# Rate control buffer aggressiveness (virgola mobile)
#sout-avcodec-rc-buffer-aggressivity=1.000000

# I quantization factor (virgola mobile)
#sout-avcodec-i-quant-factor=0.000000

# Noise reduction (intero)
#sout-avcodec-noise-reduction=0

# MPEG4 quantization matrix (booleano)
#sout-avcodec-mpeg4-matrix=0

# Minimum video quantizer scale (intero)
#sout-avcodec-qmin=0

# Maximum video quantizer scale (intero)
#sout-avcodec-qmax=0

# Trellis quantization (booleano)
#sout-avcodec-trellis=0

# Fixed quantizer scale (virgola mobile)
#sout-avcodec-qscale=3.000000

# Strict standard compliance (intero)
#sout-avcodec-strict=0

# Luminance masking (virgola mobile)
#sout-avcodec-lumi-masking=0.000000

# Darkness masking (virgola mobile)
#sout-avcodec-dark-masking=0.000000

# Motion masking (virgola mobile)
#sout-avcodec-p-masking=0.000000

# Border masking (virgola mobile)
#sout-avcodec-border-masking=0.000000

# Luminance elimination (intero)
#sout-avcodec-luma-elim-threshold=0

# Chrominance elimination (intero)
#sout-avcodec-chroma-elim-threshold=0

# Specify AAC audio profile to use (stringa)
#sout-avcodec-aac-profile=low

# Advanced options (stringa)
#sout-avcodec-options=

# Format name (stringa)
#avformat-format=

# Advanced options (stringa)
#avformat-options=

# Avformat mux (stringa)
#sout-avformat-mux=

# Advanced options (stringa)
#sout-avformat-options=

# Reset timestamps (booleano)
#sout-avformat-reset-ts=1

# Advanced options (stringa)
#avio-options=

# Advanced options (stringa)
#sout-avio-options=

[aribsub] # ARIB subtitles decoder

# Ignore ruby (furigana) (booleano)
#aribsub-ignore-ruby=0

# Use Core Text renderer (booleano)
#aribsub-use-coretext=0

[waveout] # WaveOut audio output

# Select Audio Device (stringa)
#waveout-audio-device=wavemapper

# Audio volume (virgola mobile)
#waveout-volume=1.000000

# Use float32 output (booleano)
#waveout-float32=1

# Audio output channels (intero)
#waveout-audio-channels=9

[mmdevice] # Windows Multimedia Device output

# Output back-end (stringa)
#mmdevice-backend=any

# HDMI/SPDIF audio passthrough (intero)
#mmdevice-passthrough=0

# Output device (stringa)
#mmdevice-audio-device=

# Audio volume (virgola mobile)
#mmdevice-volume=1.000000

[directsound] # DirectX audio output

# Output device (stringa)
#directx-audio-device=

# Use float32 output (booleano)
#directx-audio-float32=1

# Speaker configuration (stringa)
#directx-audio-speaker=Windows default

# Audio volume (virgola mobile)
#directx-volume=1.000000

[amem] # Audio memory output

# Sample format (stringa)
#amem-format=S16N

# Sample rate (intero)
#amem-rate=44100

# Channels count (intero)
#amem-channels=2

[afile] # File audio output

# Output file (stringa)
#audiofile-file=audiofile.wav

# Output format (stringa)
#audiofile-format=s16

# Number of output channels (intero)
#audiofile-channels=0

# Add WAVE header (booleano)
#audiofile-wav=1

[stereo_widen] # Simple stereo widening effect

# Delay time (virgola mobile)
#stereowiden-delay=20.000000

# Feedback gain (virgola mobile)
#stereowiden-feedback=0.300000

# Crossfeed (virgola mobile)
#stereowiden-crossfeed=0.300000

# Dry mix (virgola mobile)
#stereowiden-dry-mix=0.800000

[speex_resampler] # Speex resampler

# Resampling quality (intero)
#speex-resampler-quality=4

[spatializer] # Audio Spatializer

# Room size (virgola mobile)
#spatializer-roomsize=0.850000

# Room width (virgola mobile)
#spatializer-width=1.000000

# Wet (virgola mobile)
#spatializer-wet=0.400000

# Dry (virgola mobile)
#spatializer-dry=0.500000

# Damp (virgola mobile)
#spatializer-damp=0.500000

[spatialaudio] # Ambisonics renderer and binauralizer

# Headphones mode (binaural) (booleano)
#spatialaudio-headphones=0

# HRTF file for the binauralization (stringa)
#hrtf-file=

[scaletempo] # Audio tempo scaler synched with rate

# Stride Length (intero)
#scaletempo-stride=30

# Overlap Length (virgola mobile)
#scaletempo-overlap=0.200000

# Search Length (intero)
#scaletempo-search=14

[scaletempo_pitch] # Pitch Shifter

# Stride Length (intero)
#scaletempo-stride=30

# Overlap Length (virgola mobile)
#scaletempo-overlap=0.200000

# Search Length (intero)
#scaletempo-search=14

# Pitch Shift (virgola mobile)
#pitch-shift=0.000000

[samplerate] # Secret Rabbit Code (libsamplerate) resampler

# Sample rate converter type (intero)
#src-converter-type=2

[remap] # Audio channel remapper

# Left (intero)
#aout-remap-channel-left=0

# Center (intero)
#aout-remap-channel-center=1

# Right (intero)
#aout-remap-channel-right=2

# Rear left (intero)
#aout-remap-channel-rearleft=3

# Rear center (intero)
#aout-remap-channel-rearcenter=4

# Rear right (intero)
#aout-remap-channel-rearright=5

# Side left (intero)
#aout-remap-channel-middleleft=6

# Side right (intero)
#aout-remap-channel-middleright=7

# Low-frequency effects (intero)
#aout-remap-channel-lfe=8

# Normalize channels (booleano)
#aout-remap-normalize=1

[param_eq] # Parametric Equalizer

# Low freq (Hz) (virgola mobile)
#param-eq-lowf=100.000000

# Low freq gain (dB) (virgola mobile)
#param-eq-lowgain=0.000000

# High freq (Hz) (virgola mobile)
#param-eq-highf=10000.000000

# High freq gain (dB) (virgola mobile)
#param-eq-highgain=0.000000

# Freq 1 (Hz) (virgola mobile)
#param-eq-f1=300.000000

# Freq 1 gain (dB) (virgola mobile)
#param-eq-gain1=0.000000

# Freq 1 Q (virgola mobile)
#param-eq-q1=3.000000

# Freq 2 (Hz) (virgola mobile)
#param-eq-f2=1000.000000

# Freq 2 gain (dB) (virgola mobile)
#param-eq-gain2=0.000000

# Freq 2 Q (virgola mobile)
#param-eq-q2=3.000000

# Freq 3 (Hz) (virgola mobile)
#param-eq-f3=3000.000000

# Freq 3 gain (dB) (virgola mobile)
#param-eq-gain3=0.000000

# Freq 3 Q (virgola mobile)
#param-eq-q3=3.000000

[normvol] # Volume normalizer

# Number of audio buffers (intero)
#norm-buff-size=20

# Maximal volume level (virgola mobile)
#norm-max-level=2.000000

[mono] # Stereo to mono downmixer

# Use downmix algorithm (booleano)
#sout-mono-downmix=1

# Select channel to keep (intero)
#sout-mono-channel=-1

[headphone] # Headphone virtual spatialization effect

# Characteristic dimension (intero)
#headphone-dim=10

# Compensate delay (booleano)
#headphone-compensate=0

# No decoding of Dolby Surround (booleano)
#headphone-dolby=0

[gain] # Gain control filter

# Gain multiplier (virgola mobile)
#gain-value=1.000000

[equalizer] # Equalizer with 10 bands

# Equalizer preset (stringa)
#equalizer-preset=flat

# Bands gain (stringa)
#equalizer-bands=

# Two pass (booleano)
#equalizer-2pass=0

# Use VLC frequency bands (booleano)
#equalizer-vlcfreqs=1

# Global gain (virgola mobile)
#equalizer-preamp=12.000000

[compressor] # Dynamic range compressor

# RMS/peak (virgola mobile)
#compressor-rms-peak=0.200000

# Attack time (virgola mobile)
#compressor-attack=25.000000

# Release time (virgola mobile)
#compressor-release=100.000000

# Threshold level (virgola mobile)
#compressor-threshold=-11.000000

# Ratio (virgola mobile)
#compressor-ratio=4.000000

# Knee radius (virgola mobile)
#compressor-knee=5.000000

# Makeup gain (virgola mobile)
#compressor-makeup-gain=7.000000

[chorus_flanger] # Sound Delay

# Delay time (virgola mobile)
#delay-time=20.000000

# Sweep Depth (virgola mobile)
#sweep-depth=6.000000

# Sweep Rate (virgola mobile)
#sweep-rate=6.000000

# Feedback gain (virgola mobile)
#feedback-gain=0.500000

# Wet mix (virgola mobile)
#wet-mix=0.400000

# Dry Mix (virgola mobile)
#dry-mix=0.400000

[audiobargraph_a] # Audio part of the BarGraph function

# Defines if BarGraph information should be sent (intero)
#audiobargraph_a-bargraph=1

# Sends the barGraph information every n audio packets (intero)
#audiobargraph_a-bargraph_repetition=4

# Defines if silence alarm information should be sent (intero)
#audiobargraph_a-silence=1

# Time window to use in ms (intero)
#audiobargraph_a-time_window=5000

# Minimum Audio level to raise the alarm (virgola mobile)
#audiobargraph_a-alarm_threshold=0.020000

# Time between two alarm messages in ms (intero)
#audiobargraph_a-repetition_time=2000

[udp] # UDP stream output

# Caching value (ms) (intero)
#sout-udp-caching=300

# Group packets (intero)
#sout-udp-group=1

[access_output_srt] # SRT stream output

# Return poll wait after timeout milliseconds (-1 = infinite) (intero)
#poll-timeout=-1

# SRT latency (ms) (intero)
#latency=120

# Password for stream encryption (stringa)
#passphrase=

# SRT maximum payload size (bytes) (intero)
#payload-size=1316

# SRT maximum bandwidth ceiling (bytes) (intero)
#bandwidth-overhead-limit=25

# Crypto key length in bytes (intero)
#key-length=16

#  SRT Stream ID (stringa)
#streamid=

[access_output_shout] # IceCAST output

# Stream name (stringa)
#sout-shout-name=VLC media player - Live stream

# Stream description (stringa)
#sout-shout-description=Live stream from VLC media player

# Stream MP3 (booleano)
#sout-shout-mp3=0

# Genre description (stringa)
#sout-shout-genre=Alternative

# URL description (stringa)
#sout-shout-url=http://www.videolan.org/vlc

# Bitrate (stringa)
#sout-shout-bitrate=

# Samplerate (stringa)
#sout-shout-samplerate=

# Number of channels (stringa)
#sout-shout-channels=

# Ogg Vorbis Quality (stringa)
#sout-shout-quality=

# Stream public (booleano)
#sout-shout-public=0

[access_output_rist] # RIST stream output

# RIST target packet size (bytes) (intero)
#sout-rist-packet-size=1328

# RIST data output caching size (ms) (intero)
#sout-rist-caching=50

# RIST retry-buffer queue size (ms) (intero)
#sout-rist-buffer-size=0

# SSRC used in RTP output (default is random, i.e. 0) (intero)
#sout-rist-ssrc=0

# Stream name (stringa)
#sout-rist-stream-name=

[access_output_livehttp] # HTTP Live streaming output

# Segment length (intero)
#sout-livehttp-seglen=10

# Number of segments (intero)
#sout-livehttp-numsegs=0

# Number of first segment (intero)
#sout-livehttp-initial-segment-number=1

# Split segments anywhere (booleano)
#sout-livehttp-splitanywhere=0

# Delete segments (booleano)
#sout-livehttp-delsegs=1

# Use muxers rate control mechanism (booleano)
#sout-livehttp-ratecontrol=0

# Allow cache (booleano)
#sout-livehttp-caching=0

# Use randomized IV for encryption (booleano)
#sout-livehttp-generate-iv=0

# Index file (stringa)
#sout-livehttp-index=

# Full URL to put in index file (stringa)
#sout-livehttp-index-url=

# AES key URI to place in playlist (stringa)
#sout-livehttp-key-uri=

# AES key file (stringa)
#sout-livehttp-key-file=

# File where vlc reads key-uri and keyfile-location (stringa)
#sout-livehttp-key-loadfile=

[http] # HTTP stream output

# Username (stringa)
#sout-http-user=

# Password (stringa)
#sout-http-pwd=

# Mime (stringa)
#sout-http-mime=

# Metacube (booleano)
#sout-http-metacube=0

[file] # File stream output

# Overwrite existing file (booleano)
#sout-file-overwrite=1

# Append to file (booleano)
#sout-file-append=0

# Format time and date (booleano)
#sout-file-format=0

[vnc] # VNC client access

# Username (stringa)
#rfb-user=

# Password (stringa)
#rfb-password=

# X.509 Certificate Authority (stringa)
#rfb-x509-ca=

# X.509 Certificate Revocation List (stringa)
#rfb-x509-crl=

# X.509 Client certificate (stringa)
#rfb-x509-client-cert=

# X.509 Client private key (stringa)
#rfb-x509-client-key=

# Frame rate (virgola mobile)
#rfb-fps=5.000000

# Frame buffer depth (stringa)
#rfb-chroma=RV32

# Compression level (intero)
#rfb-compress-level=0

# Image quality (intero)
#rfb-quality-level=9

[vdr] # VDR recordings

# Chapter offset in ms (intero)
#vdr-chapter-offset=0

# Frame rate (virgola mobile)
#vdr-fps=25.000000

[udp] # UDP input

# UDP Source timeout (sec) (intero)
#udp-timeout=-1

[timecode] # Time code subpicture elementary stream generator

# Frame rate (stringa)
#timecode-fps=25/1

[smb] # SMB input

# Username (stringa)
#smb-user=

# Password (stringa)
#smb-pwd=

# SMB domain (stringa)
#smb-domain=

[shm] # Shared memory framebuffer

# Frame rate (virgola mobile)
#shm-fps=10.000000

# Frame buffer depth (intero)
#shm-depth=0

# Frame buffer width (intero)
#shm-width=800

# Frame buffer height (intero)
#shm-height=480

[sftp] # SFTP input

# SFTP port (intero)
#sftp-port=22

# Username (stringa)
#sftp-user=

# Password (stringa)
#sftp-pwd=

[screen] # Screen Input

# Frame rate (virgola mobile)
#screen-fps=1.000000

# Region top row (intero)
#screen-top=0

# Region left column (intero)
#screen-left=0

# Capture region width (intero)
#screen-width=0

# Capture region height (intero)
#screen-height=0

# Follow the mouse (booleano)
#screen-follow-mouse=0

# Mouse pointer image (stringa)
#screen-mouse-image=

# Capture fragment size (intero)
#screen-fragment-size=0

[satip] # SAT>IP Receiver Plugin

# Receive buffer (intero)
#satip-buffer=4194304

# Request multicast stream (booleano)
#satip-multicast=0

# Host (stringa)
#satip-host=

[rtp] # Real-Time Protocol (RTP) input

# RTCP (local) port (intero)
#rtcp-port=0

# SRTP key (hexadecimal) (stringa)
#srtp-key=

# SRTP salt (hexadecimal) (stringa)
#srtp-salt=

# Maximum RTP sources (intero)
#rtp-max-src=1

# RTP source timeout (sec) (intero)
#rtp-timeout=5

# Maximum RTP sequence number dropout (intero)
#rtp-max-dropout=3000

# Maximum RTP sequence number misordering (intero)
#rtp-max-misorder=100

# RTP payload format assumed for dynamic payloads (stringa)
#rtp-dynamic-pt=

[rist] # RIST input

# RIST maximum packet size (bytes) (intero)
#packet-size=1472

# RIST demux/decode maximum jitter (default is 5ms) (intero)
#maximum-jitter=5

# RIST latency (ms) (intero)
#latency=1000

# RIST nack retry interval (ms) (intero)
#retry-interval=132

# RIST reorder buffer (ms) (intero)
#reorder-buffer=70

# RIST maximum retry count (intero)
#max-retries=10

# RIST nack type, 0 = range, 1 = bitmask. Default is range (intero)
#nack-type=0

# Disable NACK output packets (booleano)
#disable-nacks=0

# Do not check for a valid rtcp message from the encoder (booleano)
#mcast-blind-nacks=0

[nfs] # NFS input

# Set NFS uid/guid automatically (booleano)
#nfs-auto-guid=1

[live555] # RTP/RTSP/SDP demuxer (using Live555)

# Use RTP over RTSP (TCP) (booleano)
#rtsp-tcp=0

# Client port (intero)
#rtp-client-port=-1

# Force multicast RTP via RTSP (booleano)
#rtsp-mcast=0

# Tunnel RTSP and RTP over HTTP (booleano)
#rtsp-http=0

# HTTP tunnel port (intero)
#rtsp-http-port=80

# Kasenna RTSP dialect (booleano)
#rtsp-kasenna=0

# WMServer RTSP dialect (booleano)
#rtsp-wmserver=0

# Username (stringa)
#rtsp-user=

# Password (stringa)
#rtsp-pwd=

# RTSP frame buffer size (intero)
#rtsp-frame-buffer-size=250000

[libbluray] # Blu-ray Disc support (libbluray)

# Blu-ray menus (booleano)
#bluray-menu=1

# Region code (stringa)
#bluray-region=B

[access] # HTTPS input

# Cookies forwarding (booleano)
#http-forward-cookies=1

# User agent (stringa)
#http-user-agent=

[http] # HTTP input

# Auto re-connect (booleano)
#http-reconnect=0

[ftp] # FTP input

# Username (stringa)
#ftp-user=

# Password (stringa)
#ftp-pwd=

# FTP account (stringa)
#ftp-account=anonymous

[filesystem] # File input

# List special files (booleano)
#list-special-files=0

[dvdread] # DVDRead Input (no menu support)

# DVD angle (intero)
#dvdread-angle=1

[dvdnav] # DVDnav Input

# DVD angle (intero)
#dvdnav-angle=1

# Start directly in menu (booleano)
#dvdnav-menu=1

[dtv] # Digital Television and Radio

# DVB adapter (intero)
#dvb-adapter=-1

# Network name (stringa)
#dvb-network-name=

# Network name to create (stringa)
#dvb-create-name=

# Frequency (Hz) (intero)
#dvb-frequency=0

# Spectrum inversion (intero)
#dvb-inversion=-1

# Bandwidth (MHz) (intero)
#dvb-bandwidth=0

# Transmission mode (intero)
#dvb-transmission=0

# Guard interval (stringa)
#dvb-guard=

# High-priority code rate (stringa)
#dvb-code-rate-hp=

# Low-priority code rate (stringa)
#dvb-code-rate-lp=

# Hierarchy mode (intero)
#dvb-hierarchy=-1

# DVB-T2 Physical Layer Pipe (intero)
#dvb-plp-id=0

# Layer A modulation (stringa)
#dvb-a-modulation=

# Layer A code rate (stringa)
#dvb-a-fec=

# Layer A segments count (intero)
#dvb-a-count=0

# Layer A time interleaving (intero)
#dvb-a-interleaving=0

# Layer B modulation (stringa)
#dvb-b-modulation=

# Layer B code rate (stringa)
#dvb-b-fec=

# Layer B segments count (intero)
#dvb-b-count=0

# Layer B time interleaving (intero)
#dvb-b-interleaving=0

# Layer C modulation (stringa)
#dvb-c-modulation=

# Layer C code rate (stringa)
#dvb-c-fec=

# Layer C segments count (intero)
#dvb-c-count=0

# Layer C time interleaving (intero)
#dvb-c-interleaving=0

# Modulation / Constellation (stringa)
#dvb-modulation=

# Symbol rate (bauds) (intero)
#dvb-srate=0

# FEC code rate (stringa)
#dvb-fec=

# Stream identifier (intero)
#dvb-stream=0

# Pilot (intero)
#dvb-pilot=-1

# Roll-off factor (intero)
#dvb-rolloff=-1

# Transport stream ID (intero)
#dvb-ts-id=0

# Polarization (Voltage) (stringa)
#dvb-polarization=

#  (intero)
#dvb-voltage=13

# Local oscillator low frequency (kHz) (intero)
#dvb-lnb-low=0

# Local oscillator high frequency (kHz) (intero)
#dvb-lnb-high=0

# Universal LNB switch frequency (kHz) (intero)
#dvb-lnb-switch=11700000

# Network identifier (intero)
#dvb-network-id=0

# Satellite azimuth (intero)
#dvb-azimuth=0

# Satellite elevation (intero)
#dvb-elevation=0

# Satellite longitude (intero)
#dvb-longitude=0

# Satellite range code (stringa)
#dvb-range=

# Major channel (intero)
#dvb-major-channel=0

# ATSC minor channel (intero)
#dvb-minor-channel=0

# Physical channel (intero)
#dvb-physical-channel=0

[dshow] # DirectShow input

# Video device name (stringa)
#dshow-vdev=

# Audio device name (stringa)
#dshow-adev=

# Video size (stringa)
#dshow-size=

# Picture aspect-ratio n:m (stringa)
#dshow-aspect-ratio=4:3

# Video input chroma format (stringa)
#dshow-chroma=

# Video input frame rate (virgola mobile)
#dshow-fps=0.000000

# Device properties (booleano)
#dshow-config=0

# Tuner properties (booleano)
#dshow-tuner=0

# Tuner TV Channel (intero)
#dshow-tuner-channel=0

# Tuner Frequency (intero)
#dshow-tuner-frequency=0

# Tuner country code (intero)
#dshow-tuner-country=0

# Video standard (intero)
#dshow-tuner-standard=0

# Tuner input type (intero)
#dshow-tuner-input=0

# Video input pin (intero)
#dshow-video-input=-1

# Video output pin (intero)
#dshow-video-output=-1

# Audio input pin (intero)
#dshow-audio-input=-1

# Audio output pin (intero)
#dshow-audio-output=-1

# AM Tuner mode (intero)
#dshow-amtuner-mode=1

# Number of audio channels (intero)
#dshow-audio-channels=0

# Audio sample rate (intero)
#dshow-audio-samplerate=0

# Audio bits per sample (intero)
#dshow-audio-bitspersample=0

[dcp] # Digital Cinema Package module

# KDM file (stringa)
#kdm=

[cdda] # Audio CD input

# Audio CD device (stringa)
#cd-audio=D:

# Musicbrainz Server (stringa)
#musicbrainz-server=musicbrainz.org

# CDDB Server (stringa)
#cddb-server=freedb.videolan.org

# CDDB port (intero)
#cddb-port=80

[wasapi] # Windows Audio Session API input

# Loopback mode (booleano)
#wasapi-loopback=0

[access_srt] # SRT input

# Return poll wait after timeout milliseconds (-1 = infinite) (intero)
#poll-timeout=-1

# SRT latency (ms) (intero)
#latency=120

# Password for stream encryption (stringa)
#passphrase=

# Crypto key length in bytes (intero)
#key-length=16

#  SRT Stream ID (stringa)
#streamid=

[access_mms] # Microsoft Media Server (MMS) input

# TCP/UDP timeout (ms) (intero)
#mms-timeout=5000

# Force selection of all streams (booleano)
#mms-all=0

# Maximum bitrate (intero)
#mms-maxbitrate=0

[imem] # Memory input

# ID (intero)
#imem-id=-1

# Group (intero)
#imem-group=0

# Category (intero)
#imem-cat=0

# Codec (stringa)
#imem-codec=

# Language (stringa)
#imem-language=

# Sample rate (intero)
#imem-samplerate=0

# Channels count (intero)
#imem-channels=0

# Width (intero)
#imem-width=0

# Height (intero)
#imem-height=0

# Display aspect ratio (stringa)
#imem-dar=

# Frame rate (stringa)
#imem-fps=

# Size (intero)
#imem-size=0

[concat] # Concatenated inputs

# Inputs list (stringa)
#concat-list=

[core] # core program

# Enable audio (booleano)
#audio=1

# Audio gain (virgola mobile)
#gain=1.000000

# Audio output volume step (virgola mobile)
#volume-step=12.800000

# Remember the audio volume (booleano)
#volume-save=1

# Force S/PDIF support (booleano)
#spdif=0

# Force detection of Dolby Surround (intero)
#force-dolby-surround=0

# Stereo audio output mode (intero)
#stereo-mode=0

# Audio desynchronization compensation (intero)
#audio-desync=0

# Replay gain mode (stringa)
#audio-replay-gain-mode=none

# Replay preamp (virgola mobile)
#audio-replay-gain-preamp=0.000000

# Default replay gain (virgola mobile)
#audio-replay-gain-default=-7.000000

# Peak protection (booleano)
#audio-replay-gain-peak-protection=1

# Enable time stretching audio (booleano)
#audio-time-stretch=1

# Audio output module (stringa)
#aout=

# Media role (stringa)
#role=video

# Audio filters (stringa)
#audio-filter=

# Audio visualizations (stringa)
#audio-visual=none

# Audio resampler (stringa)
#audio-resampler=

# Enable video (booleano)
#video=1

# Grayscale video output (booleano)
#grayscale=0

# Fullscreen video output (booleano)
#fullscreen=0

# Embedded video (booleano)
#embedded-video=1

#  (booleano)
#xlib=1

# Drop late frames (booleano)
#drop-late-frames=1

# Skip frames (booleano)
#skip-frames=1

# Quiet synchro (booleano)
#quiet-synchro=0

# Key press events (booleano)
#keyboard-events=1

# Mouse events (booleano)
#mouse-events=1

# Always on top (booleano)
#video-on-top=0

# Enable wallpaper mode (booleano)
#video-wallpaper=0

# Disable screensaver (booleano)
#disable-screensaver=1

# Show media title on video (booleano)
#video-title-show=1

# Show video title for x milliseconds (intero)
#video-title-timeout=5000

# Position of video title (intero)
#video-title-position=8

# Hide cursor and fullscreen controller after x milliseconds (intero)
#mouse-hide-timeout=1000

# Video snapshot directory (or filename) (stringa)
snapshot-path=D:\Video

# Video snapshot file prefix (stringa)
#snapshot-prefix=vlcsnap-

# Video snapshot format (stringa)
#snapshot-format=png

# Display video snapshot preview (booleano)
#snapshot-preview=1

# Use sequential numbers instead of timestamps (booleano)
#snapshot-sequential=0

# Video snapshot width (intero)
#snapshot-width=-1

# Video snapshot height (intero)
#snapshot-height=-1

# Video width (intero)
#width=-1

# Video height (intero)
#height=-1

# Video X coordinate (intero)
#video-x=0

# Video Y coordinate (intero)
#video-y=0

# Video cropping (stringa)
#crop=

# Custom crop ratios list (stringa)
#custom-crop-ratios=

# Source aspect ratio (stringa)
#aspect-ratio=

# Video Auto Scaling (booleano)
#autoscale=1

# Monitor pixel aspect ratio (stringa)
#monitor-par=

# Custom aspect ratios list (stringa)
#custom-aspect-ratios=

# Fix HDTV height (booleano)
#hdtv-fix=1

# Window decorations (booleano)
#video-deco=1

# Video title (stringa)
#video-title=

# Video alignment (intero)
#align=0

# Zoom video (virgola mobile)
#zoom=1.000000

# Deinterlace (intero)
#deinterlace=-1

# Deinterlace mode (stringa)
#deinterlace-mode=auto

# Video output module (stringa)
#vout=

# Video filter module (stringa)
#video-filter=

# Video splitter module (stringa)
#video-splitter=

# Enable sub-pictures (booleano)
#spu=1

# On Screen Display (booleano)
#osd=1

# Text rendering module (stringa)
#text-renderer=

# Use subtitle file (stringa)
#sub-file=

# Autodetect subtitle files (booleano)
#sub-autodetect-file=1

# Subtitle autodetection fuzziness (intero)
#sub-autodetect-fuzzy=3

# Subtitle autodetection paths (stringa)
#sub-autodetect-path=.\subtitles, .\subs

# Force subtitle position (intero)
#sub-margin=0

# Subpictures source module (stringa)
#sub-source=

# Subpictures filter module (stringa)
#sub-filter=

# Program (intero)
#program=0

# Programs (stringa)
#programs=

# Audio track (intero)
#audio-track=-1

# Subtitle track (intero)
#sub-track=-1

# Audio language (stringa)
#audio-language=

# Subtitle language (stringa)
#sub-language=

# Menu language (stringa)
#menu-language=

# Audio track ID (intero)
#audio-track-id=-1

# Subtitle track ID (intero)
#sub-track-id=-1

# Preferred Closed Captions decoder (intero)
#captions=608

# Preferred video resolution (intero)
#preferred-resolution=-1

# Input repetitions (intero)
#input-repeat=0

# Start time (virgola mobile)
#start-time=0.000000

# Stop time (virgola mobile)
#stop-time=0.000000

# Run time (virgola mobile)
#run-time=0.000000

# Fast seek (booleano)
#input-fast-seek=0

# Playback speed (virgola mobile)
#rate=1.000000

# Input list (stringa)
#input-list=

# Input slave (experimental) (stringa)
#input-slave=

# Bookmarks list for a stream (stringa)
#bookmarks=

# DVD device (stringa)
#dvd=

# VCD device (stringa)
#vcd=D:

# MTU of the network interface (intero)
#mtu=1400

# TCP connection timeout (intero)
#ipv4-timeout=5000

# HTTP server address (stringa)
#http-host=

# HTTP server port (intero)
#http-port=8080

# HTTPS server port (intero)
#https-port=8443

# RTSP server address (stringa)
#rtsp-host=

# RTSP server port (intero)
#rtsp-port=554

# HTTP/TLS server certificate (stringa)
#http-cert=

# HTTP/TLS server private key (stringa)
#http-key=

# HTTP proxy (stringa)
#http-proxy=

# HTTP proxy password (stringa)
#http-proxy-pwd=

# SOCKS server (stringa)
#socks=

# SOCKS user name (stringa)
#socks-user=

# SOCKS password (stringa)
#socks-pwd=

# Title metadata (stringa)
#meta-title=

# Author metadata (stringa)
#meta-author=

# Artist metadata (stringa)
#meta-artist=

# Genre metadata (stringa)
#meta-genre=

# Copyright metadata (stringa)
#meta-copyright=

# Description metadata (stringa)
#meta-description=

# Date metadata (stringa)
#meta-date=

# URL metadata (stringa)
#meta-url=

# File caching (ms) (intero)
#file-caching=1000

# Live capture caching (ms) (intero)
#live-caching=300

# Disc caching (ms) (intero)
#disc-caching=300

# Network caching (ms) (intero)
#network-caching=1000

# Clock reference average counter (intero)
#cr-average=40

# Clock synchronisation (intero)
#clock-synchro=-1

# Clock jitter (intero)
#clock-jitter=5000

# Network synchronisation (booleano)
#network-synchronisation=0

# Record directory (stringa)
input-record-path=P:\Documents\Videos

# Prefer native stream recording (booleano)
#input-record-native=1

# Timeshift directory (stringa)
#input-timeshift-path=

# Timeshift granularity (intero)
#input-timeshift-granularity=-1

# Change title according to current media (stringa)
#input-title-format=$Z

# Disable all lua plugins (booleano)
#lua=1

# Preferred decoders list (stringa)
#codec=

# Preferred encoders list (stringa)
#encoder=

# Access module (stringa)
#access=

# Demux module (stringa)
#demux=any

# Stream filter module (stringa)
#stream-filter=

# Demux filter module (stringa)
#demux-filter=

# Default stream output chain (stringa)
#sout=

# Display while streaming (booleano)
#sout-display=0

# Keep stream output open (booleano)
#sout-keep=0

# Enable streaming of all ES (booleano)
#sout-all=1

# Enable audio stream output (booleano)
#sout-audio=1

# Enable video stream output (booleano)
#sout-video=1

# Enable SPU stream output (booleano)
#sout-spu=1

# Stream output muxer caching (ms) (intero)
#sout-mux-caching=1500

# VLM configuration file (stringa)
#vlm-conf=

# SAP announcement interval (intero)
#sap-interval=5

# Mux module (stringa)
#mux=

# Access output module (stringa)
#access_output=

# Hop limit (TTL) (intero)
#ttl=-1

# Multicast output interface (stringa)
#miface=

# DiffServ Code Point (intero)
#dscp=0

# Preferred packetizer list (stringa)
#packetizer=

# VoD server module (stringa)
#vod-server=

# Use a plugins cache (booleano)
#plugins-cache=1

# Scan for new plugins (booleano)
#plugins-scan=1

# Preferred keystore list (stringa)
#keystore=

# Increase the priority of the process (booleano)
#high-priority=0

# Clock source (stringa)
#clock-source=

# Play files randomly forever (booleano)
#random=0

# Repeat all (booleano)
#loop=0

# Repeat current item (booleano)
#repeat=0

# Play and exit (booleano)
#play-and-exit=0

# Play and stop (booleano)
#play-and-stop=0

# Play and pause (booleano)
#play-and-pause=0

# Start paused (booleano)
#start-paused=0

# Auto start (booleano)
#playlist-autostart=1

# Pause on audio communication (booleano)
#playlist-cork=1

# Allow only one running instance (booleano)
#one-instance=0

# Use only one instance when started from file manager (booleano)
#one-instance-when-started-from-file=1

# Enqueue items into playlist in one instance mode (booleano)
#playlist-enqueue=0

# Use media library (booleano)
#media-library=0

# Display playlist tree (booleano)
#playlist-tree=0

# Default stream (stringa)
#open=

# Automatically preparse items (booleano)
#auto-preparse=1

# Preparsing timeout (intero)
#preparse-timeout=5000

# Allow metadata network access (booleano)
metadata-network-access=1

# Subdirectory behavior (stringa)
#recursive=collapse

# Ignored extensions (stringa)
#ignore-filetypes=m3u,db,nfo,ini,jpg,jpeg,ljpg,gif,png,pgm,pgmyuv,pbm,pam,tga,bmp,pnm,xpm,xcf,pcx,tif,tiff,lbm,sfv,txt,sub,idx,srt,cue,ssa

# Show hidden files (booleano)
#show-hiddenfiles=0

# Services discovery modules (stringa)
#services-discovery=

# Show advanced options (booleano)
#advanced=0

# Interface interaction (booleano)
#interact=1

# Locally collect statistics (booleano)
#stats=1

# Interface module (stringa)
#intf=

# Extra interface modules (stringa)
#extraintf=

# Control interfaces (stringa)
#control=

# Mouse wheel vertical axis control (intero)
#hotkeys-y-wheel-mode=0

# Mouse wheel horizontal axis control (intero)
#hotkeys-x-wheel-mode=2

# Fullscreen (stringa)
#global-key-toggle-fullscreen=

# Fullscreen (stringa)
#key-toggle-fullscreen=f

# Exit fullscreen (stringa)
#global-key-leave-fullscreen=

# Exit fullscreen (stringa)
#key-leave-fullscreen=Esc

# Play/Pause (stringa)
#global-key-play-pause=

# Play/Pause (stringa)
#key-play-pause=Space

# Pause only (stringa)
#global-key-pause=

# Pause only (stringa)
#key-pause=Browser Stop

# Play only (stringa)
#global-key-play=

# Play only (stringa)
#key-play=Browser Refresh

# Faster (stringa)
#global-key-faster=

# Faster (stringa)
#key-faster=+

# Slower (stringa)
#global-key-slower=

# Slower (stringa)
#key-slower=-

# Normal rate (stringa)
#global-key-rate-normal=

# Normal rate (stringa)
#key-rate-normal==

# Faster (fine) (stringa)
#global-key-rate-faster-fine=

# Faster (fine) (stringa)
#key-rate-faster-fine=]

# Slower (fine) (stringa)
#global-key-rate-slower-fine=

# Slower (fine) (stringa)
#key-rate-slower-fine=[

# Next (stringa)
#global-key-next=

# Next (stringa)
#key-next=n

# Previous (stringa)
#global-key-prev=

# Previous (stringa)
#key-prev=p

# Stop (stringa)
#global-key-stop=

# Stop (stringa)
#key-stop=s

# Position (stringa)
#global-key-position=

# Position (stringa)
#key-position=t

# Very short backwards jump (stringa)
#global-key-jump-extrashort=

# Very short backwards jump (stringa)
#key-jump-extrashort=Shift+Left

# Very short forward jump (stringa)
#global-key-jump+extrashort=

# Very short forward jump (stringa)
#key-jump+extrashort=Shift+Right

# Short backwards jump (stringa)
#global-key-jump-short=

# Short backwards jump (stringa)
#key-jump-short=Alt+Left

# Short forward jump (stringa)
#global-key-jump+short=

# Short forward jump (stringa)
#key-jump+short=Alt+Right

# Medium backwards jump (stringa)
#global-key-jump-medium=

# Medium backwards jump (stringa)
#key-jump-medium=Ctrl+Left

# Medium forward jump (stringa)
#global-key-jump+medium=

# Medium forward jump (stringa)
#key-jump+medium=Ctrl+Right

# Long backwards jump (stringa)
#global-key-jump-long=

# Long backwards jump (stringa)
#key-jump-long=Ctrl+Alt+Left

# Long forward jump (stringa)
#global-key-jump+long=

# Long forward jump (stringa)
#key-jump+long=Ctrl+Alt+Right

# Next frame (stringa)
#global-key-frame-next=

# Next frame (stringa)
#key-frame-next=e

# Activate (stringa)
#global-key-nav-activate=

# Activate (stringa)
#key-nav-activate=Enter

# Navigate up (stringa)
#global-key-nav-up=

# Navigate up (stringa)
#key-nav-up=Up

# Navigate down (stringa)
#global-key-nav-down=

# Navigate down (stringa)
#key-nav-down=Down

# Navigate left (stringa)
#global-key-nav-left=

# Navigate left (stringa)
#key-nav-left=Left

# Navigate right (stringa)
#global-key-nav-right=

# Navigate right (stringa)
#key-nav-right=Right

# Go to the DVD menu (stringa)
#global-key-disc-menu=

# Go to the DVD menu (stringa)
#key-disc-menu=Shift+m

# Select previous DVD title (stringa)
#global-key-title-prev=

# Select previous DVD title (stringa)
#key-title-prev=Shift+o

# Select next DVD title (stringa)
#global-key-title-next=

# Select next DVD title (stringa)
#key-title-next=Shift+b

# Select prev DVD chapter (stringa)
#global-key-chapter-prev=

# Select prev DVD chapter (stringa)
#key-chapter-prev=Shift+p

# Select next DVD chapter (stringa)
#global-key-chapter-next=

# Select next DVD chapter (stringa)
#key-chapter-next=Shift+n

# Quit (stringa)
#global-key-quit=

# Quit (stringa)
#key-quit=Ctrl+q

# Volume up (stringa)
#global-key-vol-up=

# Volume up (stringa)
#key-vol-up=Ctrl+Up

# Volume down (stringa)
#global-key-vol-down=

# Volume down (stringa)
#key-vol-down=Ctrl+Down

# Mute (stringa)
#global-key-vol-mute=

# Mute (stringa)
#key-vol-mute=m

# Subtitle delay up (stringa)
#global-key-subdelay-up=

# Subtitle delay up (stringa)
#key-subdelay-up=h

# Subtitle delay down (stringa)
#global-key-subdelay-down=

# Subtitle delay down (stringa)
#key-subdelay-down=g

# Subtitle sync / bookmark audio timestamp (stringa)
#global-key-subsync-markaudio=

# Subtitle sync / bookmark audio timestamp (stringa)
#key-subsync-markaudio=Shift+h

# Subtitle sync / bookmark subtitle timestamp (stringa)
#global-key-subsync-marksub=

# Subtitle sync / bookmark subtitle timestamp (stringa)
#key-subsync-marksub=Shift+j

# Subtitle sync / synchronize audio & subtitle timestamps (stringa)
#global-key-subsync-apply=

# Subtitle sync / synchronize audio & subtitle timestamps (stringa)
#key-subsync-apply=Shift+k

# Subtitle sync / reset audio & subtitle synchronization (stringa)
#global-key-subsync-reset=

# Subtitle sync / reset audio & subtitle synchronization (stringa)
#key-subsync-reset=Ctrl+Shift+k

# Subtitle position up (stringa)
#global-key-subpos-up=

# Subtitle position up (stringa)
#key-subpos-up=

# Subtitle position down (stringa)
#global-key-subpos-down=

# Subtitle position down (stringa)
#key-subpos-down=

# Audio delay up (stringa)
#global-key-audiodelay-up=

# Audio delay up (stringa)
#key-audiodelay-up=k

# Audio delay down (stringa)
#global-key-audiodelay-down=

# Audio delay down (stringa)
#key-audiodelay-down=j

# Cycle audio track (stringa)
#global-key-audio-track=

# Cycle audio track (stringa)
#key-audio-track=b

# Cycle through audio devices (stringa)
#global-key-audiodevice-cycle=

# Cycle through audio devices (stringa)
#key-audiodevice-cycle=Shift+a

# Cycle subtitle track in reverse order (stringa)
#global-key-subtitle-revtrack=

# Cycle subtitle track in reverse order (stringa)
#key-subtitle-revtrack=Alt+v

# Cycle subtitle track (stringa)
#global-key-subtitle-track=

# Cycle subtitle track (stringa)
#key-subtitle-track=v

# Toggle subtitles (stringa)
#global-key-subtitle-toggle=

# Toggle subtitles (stringa)
#key-subtitle-toggle=Shift+v

# Cycle next program Service ID (stringa)
#global-key-program-sid-next=

# Cycle next program Service ID (stringa)
#key-program-sid-next=x

# Cycle previous program Service ID (stringa)
#global-key-program-sid-prev=

# Cycle previous program Service ID (stringa)
#key-program-sid-prev=Shift+x

# Cycle source aspect ratio (stringa)
#global-key-aspect-ratio=

# Cycle source aspect ratio (stringa)
#key-aspect-ratio=a

# Cycle video crop (stringa)
#global-key-crop=

# Cycle video crop (stringa)
#key-crop=c

# Toggle autoscaling (stringa)
#global-key-toggle-autoscale=

# Toggle autoscaling (stringa)
#key-toggle-autoscale=o

# Increase scale factor (stringa)
#global-key-incr-scalefactor=

# Increase scale factor (stringa)
#key-incr-scalefactor=Alt+o

# Decrease scale factor (stringa)
#global-key-decr-scalefactor=

# Decrease scale factor (stringa)
#key-decr-scalefactor=Alt+Shift+o

# Toggle deinterlacing (stringa)
#global-key-deinterlace=

# Toggle deinterlacing (stringa)
#key-deinterlace=d

# Cycle deinterlace modes (stringa)
#global-key-deinterlace-mode=

# Cycle deinterlace modes (stringa)
#key-deinterlace-mode=Shift+d

# Show controller in fullscreen (stringa)
#global-key-intf-show=

# Show controller in fullscreen (stringa)
#key-intf-show=i

# Boss key (stringa)
#global-key-intf-boss=

# Boss key (stringa)
#key-intf-boss=

# Context menu (stringa)
#global-key-intf-popup-menu=

# Context menu (stringa)
#key-intf-popup-menu=Menu

# Take video snapshot (stringa)
#global-key-snapshot=

# Take video snapshot (stringa)
#key-snapshot=Shift+s

# Record (stringa)
#global-key-record=

# Record (stringa)
#key-record=Shift+r

# Zoom (stringa)
#global-key-zoom=

# Zoom (stringa)
#key-zoom=z

# Un-Zoom (stringa)
#global-key-unzoom=

# Un-Zoom (stringa)
#key-unzoom=Shift+z

# Toggle wallpaper mode in video output (stringa)
#global-key-wallpaper=

# Toggle wallpaper mode in video output (stringa)
#key-wallpaper=w

# Crop one pixel from the top of the video (stringa)
#global-key-crop-top=

# Crop one pixel from the top of the video (stringa)
#key-crop-top=Alt+r

# Uncrop one pixel from the top of the video (stringa)
#global-key-uncrop-top=

# Uncrop one pixel from the top of the video (stringa)
#key-uncrop-top=Alt+Shift+r

# Crop one pixel from the left of the video (stringa)
#global-key-crop-left=

# Crop one pixel from the left of the video (stringa)
#key-crop-left=Alt+d

# Uncrop one pixel from the left of the video (stringa)
#global-key-uncrop-left=

# Uncrop one pixel from the left of the video (stringa)
#key-uncrop-left=Alt+Shift+d

# Crop one pixel from the bottom of the video (stringa)
#global-key-crop-bottom=

# Crop one pixel from the bottom of the video (stringa)
#key-crop-bottom=Alt+c

# Uncrop one pixel from the bottom of the video (stringa)
#global-key-uncrop-bottom=

# Uncrop one pixel from the bottom of the video (stringa)
#key-uncrop-bottom=Alt+Shift+c

# Crop one pixel from the right of the video (stringa)
#global-key-crop-right=

# Crop one pixel from the right of the video (stringa)
#key-crop-right=Alt+f

# Uncrop one pixel from the right of the video (stringa)
#global-key-uncrop-right=

# Uncrop one pixel from the right of the video (stringa)
#key-uncrop-right=Alt+Shift+f

# Random (stringa)
#global-key-random=

# Random (stringa)
#key-random=r

# Normal/Loop/Repeat (stringa)
#global-key-loop=

# Normal/Loop/Repeat (stringa)
#key-loop=l

# Shrink the viewpoint field of view (360°) (stringa)
#global-key-viewpoint-fov-in=

# Shrink the viewpoint field of view (360°) (stringa)
#key-viewpoint-fov-in=Page Up

# Expand the viewpoint field of view (360°) (stringa)
#global-key-viewpoint-fov-out=

# Expand the viewpoint field of view (360°) (stringa)
#key-viewpoint-fov-out=Page Down

# Roll the viewpoint clockwise (360°) (stringa)
#global-key-viewpoint-roll-clock=

# Roll the viewpoint clockwise (360°) (stringa)
#key-viewpoint-roll-clock=

# Roll the viewpoint anti-clockwise (360°) (stringa)
#global-key-viewpoint-roll-anticlock=

# Roll the viewpoint anti-clockwise (360°) (stringa)
#key-viewpoint-roll-anticlock=

# 1:4 Quarter (stringa)
#global-key-zoom-quarter=

# 1:4 Quarter (stringa)
#key-zoom-quarter=Alt+1

# 1:2 Half (stringa)
#global-key-zoom-half=

# 1:2 Half (stringa)
#key-zoom-half=Alt+2

# 1:1 Original (stringa)
#global-key-zoom-original=

# 1:1 Original (stringa)
#key-zoom-original=Alt+3

# 2:1 Double (stringa)
#global-key-zoom-double=

# 2:1 Double (stringa)
#key-zoom-double=Alt+4

# Very short jump length (intero)
#extrashort-jump-size=3

# Short jump length (intero)
#short-jump-size=10

# Medium jump length (intero)
#medium-jump-size=60

# Long jump length (intero)
#long-jump-size=300

# Set playlist bookmark 1 (stringa)
#global-key-set-bookmark1=

# Set playlist bookmark 1 (stringa)
#key-set-bookmark1=Ctrl+F1

# Set playlist bookmark 2 (stringa)
#global-key-set-bookmark2=

# Set playlist bookmark 2 (stringa)
#key-set-bookmark2=Ctrl+F2

# Set playlist bookmark 3 (stringa)
#global-key-set-bookmark3=

# Set playlist bookmark 3 (stringa)
#key-set-bookmark3=Ctrl+F3

# Set playlist bookmark 4 (stringa)
#global-key-set-bookmark4=

# Set playlist bookmark 4 (stringa)
#key-set-bookmark4=Ctrl+F4

# Set playlist bookmark 5 (stringa)
#global-key-set-bookmark5=

# Set playlist bookmark 5 (stringa)
#key-set-bookmark5=Ctrl+F5

# Set playlist bookmark 6 (stringa)
#global-key-set-bookmark6=

# Set playlist bookmark 6 (stringa)
#key-set-bookmark6=Ctrl+F6

# Set playlist bookmark 7 (stringa)
#global-key-set-bookmark7=

# Set playlist bookmark 7 (stringa)
#key-set-bookmark7=Ctrl+F7

# Set playlist bookmark 8 (stringa)
#global-key-set-bookmark8=

# Set playlist bookmark 8 (stringa)
#key-set-bookmark8=Ctrl+F8

# Set playlist bookmark 9 (stringa)
#global-key-set-bookmark9=

# Set playlist bookmark 9 (stringa)
#key-set-bookmark9=Ctrl+F9

# Set playlist bookmark 10 (stringa)
#global-key-set-bookmark10=

# Set playlist bookmark 10 (stringa)
#key-set-bookmark10=Ctrl+F10

# Play playlist bookmark 1 (stringa)
#global-key-play-bookmark1=

# Play playlist bookmark 1 (stringa)
#key-play-bookmark1=F1

# Play playlist bookmark 2 (stringa)
#global-key-play-bookmark2=

# Play playlist bookmark 2 (stringa)
#key-play-bookmark2=F2

# Play playlist bookmark 3 (stringa)
#global-key-play-bookmark3=

# Play playlist bookmark 3 (stringa)
#key-play-bookmark3=F3

# Play playlist bookmark 4 (stringa)
#global-key-play-bookmark4=

# Play playlist bookmark 4 (stringa)
#key-play-bookmark4=F4

# Play playlist bookmark 5 (stringa)
#global-key-play-bookmark5=

# Play playlist bookmark 5 (stringa)
#key-play-bookmark5=F5

# Play playlist bookmark 6 (stringa)
#global-key-play-bookmark6=

# Play playlist bookmark 6 (stringa)
#key-play-bookmark6=F6

# Play playlist bookmark 7 (stringa)
#global-key-play-bookmark7=

# Play playlist bookmark 7 (stringa)
#key-play-bookmark7=F7

# Play playlist bookmark 8 (stringa)
#global-key-play-bookmark8=

# Play playlist bookmark 8 (stringa)
#key-play-bookmark8=F8

# Play playlist bookmark 9 (stringa)
#global-key-play-bookmark9=

# Play playlist bookmark 9 (stringa)
#key-play-bookmark9=F9

# Play playlist bookmark 10 (stringa)
#global-key-play-bookmark10=

# Play playlist bookmark 10 (stringa)
#key-play-bookmark10=F10

# Clear the playlist (stringa)
#global-key-clear-playlist=

# Clear the playlist (stringa)
#key-clear-playlist=Ctrl+w

# Reset subtitles text scale (stringa)
#global-key-subtitle-text-scale-normal=

# Reset subtitles text scale (stringa)
#key-subtitle-text-scale-normal=Ctrl+0

# Scale down subtitles text (stringa)
#global-key-subtitle-text-scale-up=

# Scale down subtitles text (stringa)
#key-subtitle-text-scale-up=Ctrl+Mouse Wheel Up

# Scale up subtitles text (stringa)
#global-key-subtitle-text-scale-down=

# Scale up subtitles text (stringa)
#key-subtitle-text-scale-down=Ctrl+Mouse Wheel Down

# Playlist bookmark 1 (stringa)
#bookmark1=

# Playlist bookmark 2 (stringa)
#bookmark2=

# Playlist bookmark 3 (stringa)
#bookmark3=

# Playlist bookmark 4 (stringa)
#bookmark4=

# Playlist bookmark 5 (stringa)
#bookmark5=

# Playlist bookmark 6 (stringa)
#bookmark6=

# Playlist bookmark 7 (stringa)
#bookmark7=

# Playlist bookmark 8 (stringa)
#bookmark8=

# Playlist bookmark 9 (stringa)
#bookmark9=

# Playlist bookmark 10 (stringa)
#bookmark10=

